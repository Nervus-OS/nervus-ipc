# 注意：protocol/**/*.pb.go 是【故意提交】的，不要往这里加忽略规则。
# 理由见 README「生成物提交进仓库」。

# 本地构建产物
/bin/
/build/

# JVM
.gradle/
jvm/**/build/
# Kotlin 编译器的会话与错误日志。文件名带时间戳，每次构建都产出新的一批，
# 提交进来只会制造无穷的 diff 噪音，且对任何人都没有诊断价值（真出错时
# 日志就在本地）。曾经有三个 errors-*.log 混进了 git。
.kotlin/

# 编辑器
.idea/
.vscode/
*.swp
