// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: nervus/interface/permission/v1/permission_ui.proto
// Protobuf Java Version: 4.29.3

package io.github.nervusos.iface.permission.v1;

public interface RequestPermissionRequestOrBuilder extends
    // @@protoc_insertion_point(interface_extends:nervus.interface.permission.v1.RequestPermissionRequest)
    com.google.protobuf.MessageOrBuilder {

  /**
   * <pre>
   * 要申请的权限 ID。
   *
   * 一次可以申请多条，弹窗把它们列在一起让用户逐条决定。批量而不是逐条弹窗
   * 是有意的：一个功能往往同时需要摄像头与麦克风，连着弹两次窗会让用户在
   * 不知道还有几次的情况下做决定，也更容易被无脑点掉。
   *
   * 空列表是错误（INVALID_ARGUMENT）而不是空操作：与 Check 不同，弹一个没有
   * 任何条目的窗对用户没有意义，那只能是调用方的 bug。
   * </pre>
   *
   * <code>repeated string permission_ids = 1 [json_name = "permissionIds"];</code>
   * @return A list containing the permissionIds.
   */
  java.util.List<java.lang.String>
      getPermissionIdsList();
  /**
   * <pre>
   * 要申请的权限 ID。
   *
   * 一次可以申请多条，弹窗把它们列在一起让用户逐条决定。批量而不是逐条弹窗
   * 是有意的：一个功能往往同时需要摄像头与麦克风，连着弹两次窗会让用户在
   * 不知道还有几次的情况下做决定，也更容易被无脑点掉。
   *
   * 空列表是错误（INVALID_ARGUMENT）而不是空操作：与 Check 不同，弹一个没有
   * 任何条目的窗对用户没有意义，那只能是调用方的 bug。
   * </pre>
   *
   * <code>repeated string permission_ids = 1 [json_name = "permissionIds"];</code>
   * @return The count of permissionIds.
   */
  int getPermissionIdsCount();
  /**
   * <pre>
   * 要申请的权限 ID。
   *
   * 一次可以申请多条，弹窗把它们列在一起让用户逐条决定。批量而不是逐条弹窗
   * 是有意的：一个功能往往同时需要摄像头与麦克风，连着弹两次窗会让用户在
   * 不知道还有几次的情况下做决定，也更容易被无脑点掉。
   *
   * 空列表是错误（INVALID_ARGUMENT）而不是空操作：与 Check 不同，弹一个没有
   * 任何条目的窗对用户没有意义，那只能是调用方的 bug。
   * </pre>
   *
   * <code>repeated string permission_ids = 1 [json_name = "permissionIds"];</code>
   * @param index The index of the element to return.
   * @return The permissionIds at the given index.
   */
  java.lang.String getPermissionIds(int index);
  /**
   * <pre>
   * 要申请的权限 ID。
   *
   * 一次可以申请多条，弹窗把它们列在一起让用户逐条决定。批量而不是逐条弹窗
   * 是有意的：一个功能往往同时需要摄像头与麦克风，连着弹两次窗会让用户在
   * 不知道还有几次的情况下做决定，也更容易被无脑点掉。
   *
   * 空列表是错误（INVALID_ARGUMENT）而不是空操作：与 Check 不同，弹一个没有
   * 任何条目的窗对用户没有意义，那只能是调用方的 bug。
   * </pre>
   *
   * <code>repeated string permission_ids = 1 [json_name = "permissionIds"];</code>
   * @param index The index of the value to return.
   * @return The bytes of the permissionIds at the given index.
   */
  com.google.protobuf.ByteString
      getPermissionIdsBytes(int index);

  /**
   * <pre>
   * 面向用户的一句话理由，说明这个功能为什么需要这些权限。
   *
   * 【由应用给出而不是系统代写】：只有应用知道用户刚点了什么按钮。系统能说的
   * 只有权限本身的文案（那来自 Catalog 定义，弹窗一定会显示），而
   * 「为什么现在要」只有应用说得出。
   *
   * 可以为空，但强烈建议填。permissionui 会把它显示在权限清单上方，并明确标注
   * 这是应用自称的理由——**它是不受信内容**，不能让它看起来像系统的声明。
   * </pre>
   *
   * <code>string rationale = 2 [json_name = "rationale"];</code>
   * @return The rationale.
   */
  java.lang.String getRationale();
  /**
   * <pre>
   * 面向用户的一句话理由，说明这个功能为什么需要这些权限。
   *
   * 【由应用给出而不是系统代写】：只有应用知道用户刚点了什么按钮。系统能说的
   * 只有权限本身的文案（那来自 Catalog 定义，弹窗一定会显示），而
   * 「为什么现在要」只有应用说得出。
   *
   * 可以为空，但强烈建议填。permissionui 会把它显示在权限清单上方，并明确标注
   * 这是应用自称的理由——**它是不受信内容**，不能让它看起来像系统的声明。
   * </pre>
   *
   * <code>string rationale = 2 [json_name = "rationale"];</code>
   * @return The bytes for rationale.
   */
  com.google.protobuf.ByteString
      getRationaleBytes();
}
