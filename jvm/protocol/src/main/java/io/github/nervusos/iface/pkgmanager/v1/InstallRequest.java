// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: nervus/interface/pkgmanager/v1/pkg_manager.proto
// Protobuf Java Version: 4.29.3

package io.github.nervusos.iface.pkgmanager.v1;

/**
 * <pre>
 * InstallRequest 请求安装一个 .nspkg。
 * </pre>
 *
 * Protobuf type {@code nervus.interface.pkgmanager.v1.InstallRequest}
 */
public final class InstallRequest extends
    com.google.protobuf.GeneratedMessage implements
    // @@protoc_insertion_point(message_implements:nervus.interface.pkgmanager.v1.InstallRequest)
    InstallRequestOrBuilder {
private static final long serialVersionUID = 0L;
  static {
    com.google.protobuf.RuntimeVersion.validateProtobufGencodeVersion(
      com.google.protobuf.RuntimeVersion.RuntimeDomain.PUBLIC,
      /* major= */ 4,
      /* minor= */ 29,
      /* patch= */ 3,
      /* suffix= */ "",
      InstallRequest.class.getName());
  }
  // Use InstallRequest.newBuilder() to construct.
  private InstallRequest(com.google.protobuf.GeneratedMessage.Builder<?> builder) {
    super(builder);
  }
  private InstallRequest() {
    nspkgRelpath_ = "";
    consentedPermissions_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
    expectedManifestDigest_ = "";
  }

  public static final com.google.protobuf.Descriptors.Descriptor
      getDescriptor() {
    return io.github.nervusos.iface.pkgmanager.v1.PackageManagerProto.internal_static_nervus_interface_pkgmanager_v1_InstallRequest_descriptor;
  }

  @java.lang.Override
  protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
      internalGetFieldAccessorTable() {
    return io.github.nervusos.iface.pkgmanager.v1.PackageManagerProto.internal_static_nervus_interface_pkgmanager_v1_InstallRequest_fieldAccessorTable
        .ensureFieldAccessorsInitialized(
            io.github.nervusos.iface.pkgmanager.v1.InstallRequest.class, io.github.nervusos.iface.pkgmanager.v1.InstallRequest.Builder.class);
  }

  public static final int NSPKG_RELPATH_FIELD_NUMBER = 1;
  @SuppressWarnings("serial")
  private volatile java.lang.Object nspkgRelpath_ = "";
  /**
   * <pre>
   * .nspkg 在【用户文档区】(user-data) 中的相对路径。
   *
   * # 交接目录是 user-data，不是调用方的私有目录
   *
   * 此前这里写的是"调用方私有数据目录"，而那是错的：每个包的私有目录是 0700、
   * 属主为该包 UID，Provider 读不进去。于是协议与实现指的是两个不同的地方，
   * 【任何 App 发起的安装都不可能成功】——调用方能写的位置 Provider 读不到，
   * Provider 能读的位置调用方写不进。
   *
   * 用 user-data 而不是新造一个专用交接目录：它已经是"跨包共享的用户文件"
   * 那块地，用户下载或拷进来的 .nspkg 本来就落在这里，文件管理器也在这里。
   * 再造一个只会多出一次纯粹的复制，换不来任何隔离（两者都全系统可读）。
   *
   * 【刻意不是绝对路径】。绝对路径等于让调用方指定文件系统上任意位置，而
   * Provider 跑在 ProtectSystem=strict 的沙箱里，一个它读不到的绝对路径只会
   * 得到含义模糊的"打不开"。相对路径把交接位置写进协议本身。
   *
   * 必须是不含 ".." 的相对路径；Provider 与 nervud 都会再校验一次。
   * </pre>
   *
   * <code>string nspkg_relpath = 1 [json_name = "nspkgRelpath"];</code>
   * @return The nspkgRelpath.
   */
  @java.lang.Override
  public java.lang.String getNspkgRelpath() {
    java.lang.Object ref = nspkgRelpath_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      nspkgRelpath_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * .nspkg 在【用户文档区】(user-data) 中的相对路径。
   *
   * # 交接目录是 user-data，不是调用方的私有目录
   *
   * 此前这里写的是"调用方私有数据目录"，而那是错的：每个包的私有目录是 0700、
   * 属主为该包 UID，Provider 读不进去。于是协议与实现指的是两个不同的地方，
   * 【任何 App 发起的安装都不可能成功】——调用方能写的位置 Provider 读不到，
   * Provider 能读的位置调用方写不进。
   *
   * 用 user-data 而不是新造一个专用交接目录：它已经是"跨包共享的用户文件"
   * 那块地，用户下载或拷进来的 .nspkg 本来就落在这里，文件管理器也在这里。
   * 再造一个只会多出一次纯粹的复制，换不来任何隔离（两者都全系统可读）。
   *
   * 【刻意不是绝对路径】。绝对路径等于让调用方指定文件系统上任意位置，而
   * Provider 跑在 ProtectSystem=strict 的沙箱里，一个它读不到的绝对路径只会
   * 得到含义模糊的"打不开"。相对路径把交接位置写进协议本身。
   *
   * 必须是不含 ".." 的相对路径；Provider 与 nervud 都会再校验一次。
   * </pre>
   *
   * <code>string nspkg_relpath = 1 [json_name = "nspkgRelpath"];</code>
   * @return The bytes for nspkgRelpath.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getNspkgRelpathBytes() {
    java.lang.Object ref = nspkgRelpath_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      nspkgRelpath_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int CONSENTED_PERMISSIONS_FIELD_NUMBER = 2;
  @SuppressWarnings("serial")
  private com.google.protobuf.LazyStringArrayList consentedPermissions_ =
      com.google.protobuf.LazyStringArrayList.emptyList();
  /**
   * <pre>
   * 用户在安装确认屏上同意的敏感权限 ID 列表。
   *
   * # 它解决的是什么
   *
   * USER_CONSENT 权限（perm.camera.capture 一类）在安装期能进
   * GrantedPermissions，但运行期 AllowedAt 要求 GrantState == GRANTED，默认是
   * NOT_REQUESTED。也就是说一个普通应用装上之后，它申请的敏感权限【永远】拿
   * 不到——应用无法自救（SDK 没有请求权限的 API），用户也没有界面可批。本字段
   * 让安装动作把这一问补上：装之前先把该包申请的敏感权限摊给用户看，用户同意
   * 哪些，安装成功后那些权限的运行期状态就直接落成 GRANTED。
   *
   * # 谁有资格填它
   *
   * 【只有系统的确认界面】。经 IPC 装包必须穿过 nervus.interface.permission.ui
   * 的 ConfirmInstall：INSTALL 的 needs_user_confirmation 是 true，而 nervud 的
   * 那道门只对持有 perm.permission.admin 的调用方放行，全系统只有
   * nervus.permissionui 持有它。因此普通应用填不进来——它连 INSTALL 都调不到。
   *
   * # 内核还会再过滤一遍
   *
   * nervud 只对【同意清单 ∩ 安装期授予集合 ∩ USER_CONSENT】三者的交集落库，
   * 越界的条目静默忽略：一条没申请过的、或不是 USER_CONSENT 的权限出现在这里，
   * 不该因为确认界面写错就变成一次真实授予。因此本字段是「用户同意了什么」的
   * 陈述，不是「请授予什么」的命令。
   * </pre>
   *
   * <code>repeated string consented_permissions = 2 [json_name = "consentedPermissions"];</code>
   * @return A list containing the consentedPermissions.
   */
  public com.google.protobuf.ProtocolStringList
      getConsentedPermissionsList() {
    return consentedPermissions_;
  }
  /**
   * <pre>
   * 用户在安装确认屏上同意的敏感权限 ID 列表。
   *
   * # 它解决的是什么
   *
   * USER_CONSENT 权限（perm.camera.capture 一类）在安装期能进
   * GrantedPermissions，但运行期 AllowedAt 要求 GrantState == GRANTED，默认是
   * NOT_REQUESTED。也就是说一个普通应用装上之后，它申请的敏感权限【永远】拿
   * 不到——应用无法自救（SDK 没有请求权限的 API），用户也没有界面可批。本字段
   * 让安装动作把这一问补上：装之前先把该包申请的敏感权限摊给用户看，用户同意
   * 哪些，安装成功后那些权限的运行期状态就直接落成 GRANTED。
   *
   * # 谁有资格填它
   *
   * 【只有系统的确认界面】。经 IPC 装包必须穿过 nervus.interface.permission.ui
   * 的 ConfirmInstall：INSTALL 的 needs_user_confirmation 是 true，而 nervud 的
   * 那道门只对持有 perm.permission.admin 的调用方放行，全系统只有
   * nervus.permissionui 持有它。因此普通应用填不进来——它连 INSTALL 都调不到。
   *
   * # 内核还会再过滤一遍
   *
   * nervud 只对【同意清单 ∩ 安装期授予集合 ∩ USER_CONSENT】三者的交集落库，
   * 越界的条目静默忽略：一条没申请过的、或不是 USER_CONSENT 的权限出现在这里，
   * 不该因为确认界面写错就变成一次真实授予。因此本字段是「用户同意了什么」的
   * 陈述，不是「请授予什么」的命令。
   * </pre>
   *
   * <code>repeated string consented_permissions = 2 [json_name = "consentedPermissions"];</code>
   * @return The count of consentedPermissions.
   */
  public int getConsentedPermissionsCount() {
    return consentedPermissions_.size();
  }
  /**
   * <pre>
   * 用户在安装确认屏上同意的敏感权限 ID 列表。
   *
   * # 它解决的是什么
   *
   * USER_CONSENT 权限（perm.camera.capture 一类）在安装期能进
   * GrantedPermissions，但运行期 AllowedAt 要求 GrantState == GRANTED，默认是
   * NOT_REQUESTED。也就是说一个普通应用装上之后，它申请的敏感权限【永远】拿
   * 不到——应用无法自救（SDK 没有请求权限的 API），用户也没有界面可批。本字段
   * 让安装动作把这一问补上：装之前先把该包申请的敏感权限摊给用户看，用户同意
   * 哪些，安装成功后那些权限的运行期状态就直接落成 GRANTED。
   *
   * # 谁有资格填它
   *
   * 【只有系统的确认界面】。经 IPC 装包必须穿过 nervus.interface.permission.ui
   * 的 ConfirmInstall：INSTALL 的 needs_user_confirmation 是 true，而 nervud 的
   * 那道门只对持有 perm.permission.admin 的调用方放行，全系统只有
   * nervus.permissionui 持有它。因此普通应用填不进来——它连 INSTALL 都调不到。
   *
   * # 内核还会再过滤一遍
   *
   * nervud 只对【同意清单 ∩ 安装期授予集合 ∩ USER_CONSENT】三者的交集落库，
   * 越界的条目静默忽略：一条没申请过的、或不是 USER_CONSENT 的权限出现在这里，
   * 不该因为确认界面写错就变成一次真实授予。因此本字段是「用户同意了什么」的
   * 陈述，不是「请授予什么」的命令。
   * </pre>
   *
   * <code>repeated string consented_permissions = 2 [json_name = "consentedPermissions"];</code>
   * @param index The index of the element to return.
   * @return The consentedPermissions at the given index.
   */
  public java.lang.String getConsentedPermissions(int index) {
    return consentedPermissions_.get(index);
  }
  /**
   * <pre>
   * 用户在安装确认屏上同意的敏感权限 ID 列表。
   *
   * # 它解决的是什么
   *
   * USER_CONSENT 权限（perm.camera.capture 一类）在安装期能进
   * GrantedPermissions，但运行期 AllowedAt 要求 GrantState == GRANTED，默认是
   * NOT_REQUESTED。也就是说一个普通应用装上之后，它申请的敏感权限【永远】拿
   * 不到——应用无法自救（SDK 没有请求权限的 API），用户也没有界面可批。本字段
   * 让安装动作把这一问补上：装之前先把该包申请的敏感权限摊给用户看，用户同意
   * 哪些，安装成功后那些权限的运行期状态就直接落成 GRANTED。
   *
   * # 谁有资格填它
   *
   * 【只有系统的确认界面】。经 IPC 装包必须穿过 nervus.interface.permission.ui
   * 的 ConfirmInstall：INSTALL 的 needs_user_confirmation 是 true，而 nervud 的
   * 那道门只对持有 perm.permission.admin 的调用方放行，全系统只有
   * nervus.permissionui 持有它。因此普通应用填不进来——它连 INSTALL 都调不到。
   *
   * # 内核还会再过滤一遍
   *
   * nervud 只对【同意清单 ∩ 安装期授予集合 ∩ USER_CONSENT】三者的交集落库，
   * 越界的条目静默忽略：一条没申请过的、或不是 USER_CONSENT 的权限出现在这里，
   * 不该因为确认界面写错就变成一次真实授予。因此本字段是「用户同意了什么」的
   * 陈述，不是「请授予什么」的命令。
   * </pre>
   *
   * <code>repeated string consented_permissions = 2 [json_name = "consentedPermissions"];</code>
   * @param index The index of the value to return.
   * @return The bytes of the consentedPermissions at the given index.
   */
  public com.google.protobuf.ByteString
      getConsentedPermissionsBytes(int index) {
    return consentedPermissions_.getByteString(index);
  }

  public static final int EXPECTED_MANIFEST_DIGEST_FIELD_NUMBER = 3;
  @SuppressWarnings("serial")
  private volatile java.lang.Object expectedManifestDigest_ = "";
  /**
   * <pre>
   * 确认屏那次 INSPECT 回的 InspectResult.manifest_digest，原样回传。
   *
   * # 它关的是"看的是 A、装的是 B"
   *
   * .nspkg 放在跨包共享的 user-data 里，因此调用方在 INSPECT 与 INSTALL
   * 【之间】完全可以把文件换掉。两次调用各自都合法，单看任何一次都发现不了
   * ——而用户是基于 A 的权限清单点的头，consented_permissions 却会被用到 B 上。
   *
   * 带上它，内核会比对；不符即拒（PACKAGE_MANAGER_REASON_CONTENT_CHANGED）。
   *
   * 【为空表示不校验】。必须允许为空：系统装机脚本与 deploy 直接装包，从来没有
   * INSPECT 那一步，强制要求会把它们全部打断。凡是先给用户看过权限清单的
   * 调用方都【必须】带上它，否则那次确认没有约束力。
   * </pre>
   *
   * <code>string expected_manifest_digest = 3 [json_name = "expectedManifestDigest"];</code>
   * @return The expectedManifestDigest.
   */
  @java.lang.Override
  public java.lang.String getExpectedManifestDigest() {
    java.lang.Object ref = expectedManifestDigest_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = 
          (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      expectedManifestDigest_ = s;
      return s;
    }
  }
  /**
   * <pre>
   * 确认屏那次 INSPECT 回的 InspectResult.manifest_digest，原样回传。
   *
   * # 它关的是"看的是 A、装的是 B"
   *
   * .nspkg 放在跨包共享的 user-data 里，因此调用方在 INSPECT 与 INSTALL
   * 【之间】完全可以把文件换掉。两次调用各自都合法，单看任何一次都发现不了
   * ——而用户是基于 A 的权限清单点的头，consented_permissions 却会被用到 B 上。
   *
   * 带上它，内核会比对；不符即拒（PACKAGE_MANAGER_REASON_CONTENT_CHANGED）。
   *
   * 【为空表示不校验】。必须允许为空：系统装机脚本与 deploy 直接装包，从来没有
   * INSPECT 那一步，强制要求会把它们全部打断。凡是先给用户看过权限清单的
   * 调用方都【必须】带上它，否则那次确认没有约束力。
   * </pre>
   *
   * <code>string expected_manifest_digest = 3 [json_name = "expectedManifestDigest"];</code>
   * @return The bytes for expectedManifestDigest.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getExpectedManifestDigestBytes() {
    java.lang.Object ref = expectedManifestDigest_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b = 
          com.google.protobuf.ByteString.copyFromUtf8(
              (java.lang.String) ref);
      expectedManifestDigest_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  private byte memoizedIsInitialized = -1;
  @java.lang.Override
  public final boolean isInitialized() {
    byte isInitialized = memoizedIsInitialized;
    if (isInitialized == 1) return true;
    if (isInitialized == 0) return false;

    memoizedIsInitialized = 1;
    return true;
  }

  @java.lang.Override
  public void writeTo(com.google.protobuf.CodedOutputStream output)
                      throws java.io.IOException {
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(nspkgRelpath_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 1, nspkgRelpath_);
    }
    for (int i = 0; i < consentedPermissions_.size(); i++) {
      com.google.protobuf.GeneratedMessage.writeString(output, 2, consentedPermissions_.getRaw(i));
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(expectedManifestDigest_)) {
      com.google.protobuf.GeneratedMessage.writeString(output, 3, expectedManifestDigest_);
    }
    getUnknownFields().writeTo(output);
  }

  @java.lang.Override
  public int getSerializedSize() {
    int size = memoizedSize;
    if (size != -1) return size;

    size = 0;
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(nspkgRelpath_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(1, nspkgRelpath_);
    }
    {
      int dataSize = 0;
      for (int i = 0; i < consentedPermissions_.size(); i++) {
        dataSize += computeStringSizeNoTag(consentedPermissions_.getRaw(i));
      }
      size += dataSize;
      size += 1 * getConsentedPermissionsList().size();
    }
    if (!com.google.protobuf.GeneratedMessage.isStringEmpty(expectedManifestDigest_)) {
      size += com.google.protobuf.GeneratedMessage.computeStringSize(3, expectedManifestDigest_);
    }
    size += getUnknownFields().getSerializedSize();
    memoizedSize = size;
    return size;
  }

  @java.lang.Override
  public boolean equals(final java.lang.Object obj) {
    if (obj == this) {
     return true;
    }
    if (!(obj instanceof io.github.nervusos.iface.pkgmanager.v1.InstallRequest)) {
      return super.equals(obj);
    }
    io.github.nervusos.iface.pkgmanager.v1.InstallRequest other = (io.github.nervusos.iface.pkgmanager.v1.InstallRequest) obj;

    if (!getNspkgRelpath()
        .equals(other.getNspkgRelpath())) return false;
    if (!getConsentedPermissionsList()
        .equals(other.getConsentedPermissionsList())) return false;
    if (!getExpectedManifestDigest()
        .equals(other.getExpectedManifestDigest())) return false;
    if (!getUnknownFields().equals(other.getUnknownFields())) return false;
    return true;
  }

  @java.lang.Override
  public int hashCode() {
    if (memoizedHashCode != 0) {
      return memoizedHashCode;
    }
    int hash = 41;
    hash = (19 * hash) + getDescriptor().hashCode();
    hash = (37 * hash) + NSPKG_RELPATH_FIELD_NUMBER;
    hash = (53 * hash) + getNspkgRelpath().hashCode();
    if (getConsentedPermissionsCount() > 0) {
      hash = (37 * hash) + CONSENTED_PERMISSIONS_FIELD_NUMBER;
      hash = (53 * hash) + getConsentedPermissionsList().hashCode();
    }
    hash = (37 * hash) + EXPECTED_MANIFEST_DIGEST_FIELD_NUMBER;
    hash = (53 * hash) + getExpectedManifestDigest().hashCode();
    hash = (29 * hash) + getUnknownFields().hashCode();
    memoizedHashCode = hash;
    return hash;
  }

  public static io.github.nervusos.iface.pkgmanager.v1.InstallRequest parseFrom(
      java.nio.ByteBuffer data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static io.github.nervusos.iface.pkgmanager.v1.InstallRequest parseFrom(
      java.nio.ByteBuffer data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static io.github.nervusos.iface.pkgmanager.v1.InstallRequest parseFrom(
      com.google.protobuf.ByteString data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static io.github.nervusos.iface.pkgmanager.v1.InstallRequest parseFrom(
      com.google.protobuf.ByteString data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static io.github.nervusos.iface.pkgmanager.v1.InstallRequest parseFrom(byte[] data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }
  public static io.github.nervusos.iface.pkgmanager.v1.InstallRequest parseFrom(
      byte[] data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }
  public static io.github.nervusos.iface.pkgmanager.v1.InstallRequest parseFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static io.github.nervusos.iface.pkgmanager.v1.InstallRequest parseFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  public static io.github.nervusos.iface.pkgmanager.v1.InstallRequest parseDelimitedFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input);
  }

  public static io.github.nervusos.iface.pkgmanager.v1.InstallRequest parseDelimitedFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseDelimitedWithIOException(PARSER, input, extensionRegistry);
  }
  public static io.github.nervusos.iface.pkgmanager.v1.InstallRequest parseFrom(
      com.google.protobuf.CodedInputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input);
  }
  public static io.github.nervusos.iface.pkgmanager.v1.InstallRequest parseFrom(
      com.google.protobuf.CodedInputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessage
        .parseWithIOException(PARSER, input, extensionRegistry);
  }

  @java.lang.Override
  public Builder newBuilderForType() { return newBuilder(); }
  public static Builder newBuilder() {
    return DEFAULT_INSTANCE.toBuilder();
  }
  public static Builder newBuilder(io.github.nervusos.iface.pkgmanager.v1.InstallRequest prototype) {
    return DEFAULT_INSTANCE.toBuilder().mergeFrom(prototype);
  }
  @java.lang.Override
  public Builder toBuilder() {
    return this == DEFAULT_INSTANCE
        ? new Builder() : new Builder().mergeFrom(this);
  }

  @java.lang.Override
  protected Builder newBuilderForType(
      com.google.protobuf.GeneratedMessage.BuilderParent parent) {
    Builder builder = new Builder(parent);
    return builder;
  }
  /**
   * <pre>
   * InstallRequest 请求安装一个 .nspkg。
   * </pre>
   *
   * Protobuf type {@code nervus.interface.pkgmanager.v1.InstallRequest}
   */
  public static final class Builder extends
      com.google.protobuf.GeneratedMessage.Builder<Builder> implements
      // @@protoc_insertion_point(builder_implements:nervus.interface.pkgmanager.v1.InstallRequest)
      io.github.nervusos.iface.pkgmanager.v1.InstallRequestOrBuilder {
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return io.github.nervusos.iface.pkgmanager.v1.PackageManagerProto.internal_static_nervus_interface_pkgmanager_v1_InstallRequest_descriptor;
    }

    @java.lang.Override
    protected com.google.protobuf.GeneratedMessage.FieldAccessorTable
        internalGetFieldAccessorTable() {
      return io.github.nervusos.iface.pkgmanager.v1.PackageManagerProto.internal_static_nervus_interface_pkgmanager_v1_InstallRequest_fieldAccessorTable
          .ensureFieldAccessorsInitialized(
              io.github.nervusos.iface.pkgmanager.v1.InstallRequest.class, io.github.nervusos.iface.pkgmanager.v1.InstallRequest.Builder.class);
    }

    // Construct using io.github.nervusos.iface.pkgmanager.v1.InstallRequest.newBuilder()
    private Builder() {

    }

    private Builder(
        com.google.protobuf.GeneratedMessage.BuilderParent parent) {
      super(parent);

    }
    @java.lang.Override
    public Builder clear() {
      super.clear();
      bitField0_ = 0;
      nspkgRelpath_ = "";
      consentedPermissions_ =
          com.google.protobuf.LazyStringArrayList.emptyList();
      expectedManifestDigest_ = "";
      return this;
    }

    @java.lang.Override
    public com.google.protobuf.Descriptors.Descriptor
        getDescriptorForType() {
      return io.github.nervusos.iface.pkgmanager.v1.PackageManagerProto.internal_static_nervus_interface_pkgmanager_v1_InstallRequest_descriptor;
    }

    @java.lang.Override
    public io.github.nervusos.iface.pkgmanager.v1.InstallRequest getDefaultInstanceForType() {
      return io.github.nervusos.iface.pkgmanager.v1.InstallRequest.getDefaultInstance();
    }

    @java.lang.Override
    public io.github.nervusos.iface.pkgmanager.v1.InstallRequest build() {
      io.github.nervusos.iface.pkgmanager.v1.InstallRequest result = buildPartial();
      if (!result.isInitialized()) {
        throw newUninitializedMessageException(result);
      }
      return result;
    }

    @java.lang.Override
    public io.github.nervusos.iface.pkgmanager.v1.InstallRequest buildPartial() {
      io.github.nervusos.iface.pkgmanager.v1.InstallRequest result = new io.github.nervusos.iface.pkgmanager.v1.InstallRequest(this);
      if (bitField0_ != 0) { buildPartial0(result); }
      onBuilt();
      return result;
    }

    private void buildPartial0(io.github.nervusos.iface.pkgmanager.v1.InstallRequest result) {
      int from_bitField0_ = bitField0_;
      if (((from_bitField0_ & 0x00000001) != 0)) {
        result.nspkgRelpath_ = nspkgRelpath_;
      }
      if (((from_bitField0_ & 0x00000002) != 0)) {
        consentedPermissions_.makeImmutable();
        result.consentedPermissions_ = consentedPermissions_;
      }
      if (((from_bitField0_ & 0x00000004) != 0)) {
        result.expectedManifestDigest_ = expectedManifestDigest_;
      }
    }

    @java.lang.Override
    public Builder mergeFrom(com.google.protobuf.Message other) {
      if (other instanceof io.github.nervusos.iface.pkgmanager.v1.InstallRequest) {
        return mergeFrom((io.github.nervusos.iface.pkgmanager.v1.InstallRequest)other);
      } else {
        super.mergeFrom(other);
        return this;
      }
    }

    public Builder mergeFrom(io.github.nervusos.iface.pkgmanager.v1.InstallRequest other) {
      if (other == io.github.nervusos.iface.pkgmanager.v1.InstallRequest.getDefaultInstance()) return this;
      if (!other.getNspkgRelpath().isEmpty()) {
        nspkgRelpath_ = other.nspkgRelpath_;
        bitField0_ |= 0x00000001;
        onChanged();
      }
      if (!other.consentedPermissions_.isEmpty()) {
        if (consentedPermissions_.isEmpty()) {
          consentedPermissions_ = other.consentedPermissions_;
          bitField0_ |= 0x00000002;
        } else {
          ensureConsentedPermissionsIsMutable();
          consentedPermissions_.addAll(other.consentedPermissions_);
        }
        onChanged();
      }
      if (!other.getExpectedManifestDigest().isEmpty()) {
        expectedManifestDigest_ = other.expectedManifestDigest_;
        bitField0_ |= 0x00000004;
        onChanged();
      }
      this.mergeUnknownFields(other.getUnknownFields());
      onChanged();
      return this;
    }

    @java.lang.Override
    public final boolean isInitialized() {
      return true;
    }

    @java.lang.Override
    public Builder mergeFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws java.io.IOException {
      if (extensionRegistry == null) {
        throw new java.lang.NullPointerException();
      }
      try {
        boolean done = false;
        while (!done) {
          int tag = input.readTag();
          switch (tag) {
            case 0:
              done = true;
              break;
            case 10: {
              nspkgRelpath_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000001;
              break;
            } // case 10
            case 18: {
              java.lang.String s = input.readStringRequireUtf8();
              ensureConsentedPermissionsIsMutable();
              consentedPermissions_.add(s);
              break;
            } // case 18
            case 26: {
              expectedManifestDigest_ = input.readStringRequireUtf8();
              bitField0_ |= 0x00000004;
              break;
            } // case 26
            default: {
              if (!super.parseUnknownField(input, extensionRegistry, tag)) {
                done = true; // was an endgroup tag
              }
              break;
            } // default:
          } // switch (tag)
        } // while (!done)
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.unwrapIOException();
      } finally {
        onChanged();
      } // finally
      return this;
    }
    private int bitField0_;

    private java.lang.Object nspkgRelpath_ = "";
    /**
     * <pre>
     * .nspkg 在【用户文档区】(user-data) 中的相对路径。
     *
     * # 交接目录是 user-data，不是调用方的私有目录
     *
     * 此前这里写的是"调用方私有数据目录"，而那是错的：每个包的私有目录是 0700、
     * 属主为该包 UID，Provider 读不进去。于是协议与实现指的是两个不同的地方，
     * 【任何 App 发起的安装都不可能成功】——调用方能写的位置 Provider 读不到，
     * Provider 能读的位置调用方写不进。
     *
     * 用 user-data 而不是新造一个专用交接目录：它已经是"跨包共享的用户文件"
     * 那块地，用户下载或拷进来的 .nspkg 本来就落在这里，文件管理器也在这里。
     * 再造一个只会多出一次纯粹的复制，换不来任何隔离（两者都全系统可读）。
     *
     * 【刻意不是绝对路径】。绝对路径等于让调用方指定文件系统上任意位置，而
     * Provider 跑在 ProtectSystem=strict 的沙箱里，一个它读不到的绝对路径只会
     * 得到含义模糊的"打不开"。相对路径把交接位置写进协议本身。
     *
     * 必须是不含 ".." 的相对路径；Provider 与 nervud 都会再校验一次。
     * </pre>
     *
     * <code>string nspkg_relpath = 1 [json_name = "nspkgRelpath"];</code>
     * @return The nspkgRelpath.
     */
    public java.lang.String getNspkgRelpath() {
      java.lang.Object ref = nspkgRelpath_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        nspkgRelpath_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * .nspkg 在【用户文档区】(user-data) 中的相对路径。
     *
     * # 交接目录是 user-data，不是调用方的私有目录
     *
     * 此前这里写的是"调用方私有数据目录"，而那是错的：每个包的私有目录是 0700、
     * 属主为该包 UID，Provider 读不进去。于是协议与实现指的是两个不同的地方，
     * 【任何 App 发起的安装都不可能成功】——调用方能写的位置 Provider 读不到，
     * Provider 能读的位置调用方写不进。
     *
     * 用 user-data 而不是新造一个专用交接目录：它已经是"跨包共享的用户文件"
     * 那块地，用户下载或拷进来的 .nspkg 本来就落在这里，文件管理器也在这里。
     * 再造一个只会多出一次纯粹的复制，换不来任何隔离（两者都全系统可读）。
     *
     * 【刻意不是绝对路径】。绝对路径等于让调用方指定文件系统上任意位置，而
     * Provider 跑在 ProtectSystem=strict 的沙箱里，一个它读不到的绝对路径只会
     * 得到含义模糊的"打不开"。相对路径把交接位置写进协议本身。
     *
     * 必须是不含 ".." 的相对路径；Provider 与 nervud 都会再校验一次。
     * </pre>
     *
     * <code>string nspkg_relpath = 1 [json_name = "nspkgRelpath"];</code>
     * @return The bytes for nspkgRelpath.
     */
    public com.google.protobuf.ByteString
        getNspkgRelpathBytes() {
      java.lang.Object ref = nspkgRelpath_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        nspkgRelpath_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * .nspkg 在【用户文档区】(user-data) 中的相对路径。
     *
     * # 交接目录是 user-data，不是调用方的私有目录
     *
     * 此前这里写的是"调用方私有数据目录"，而那是错的：每个包的私有目录是 0700、
     * 属主为该包 UID，Provider 读不进去。于是协议与实现指的是两个不同的地方，
     * 【任何 App 发起的安装都不可能成功】——调用方能写的位置 Provider 读不到，
     * Provider 能读的位置调用方写不进。
     *
     * 用 user-data 而不是新造一个专用交接目录：它已经是"跨包共享的用户文件"
     * 那块地，用户下载或拷进来的 .nspkg 本来就落在这里，文件管理器也在这里。
     * 再造一个只会多出一次纯粹的复制，换不来任何隔离（两者都全系统可读）。
     *
     * 【刻意不是绝对路径】。绝对路径等于让调用方指定文件系统上任意位置，而
     * Provider 跑在 ProtectSystem=strict 的沙箱里，一个它读不到的绝对路径只会
     * 得到含义模糊的"打不开"。相对路径把交接位置写进协议本身。
     *
     * 必须是不含 ".." 的相对路径；Provider 与 nervud 都会再校验一次。
     * </pre>
     *
     * <code>string nspkg_relpath = 1 [json_name = "nspkgRelpath"];</code>
     * @param value The nspkgRelpath to set.
     * @return This builder for chaining.
     */
    public Builder setNspkgRelpath(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      nspkgRelpath_ = value;
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * .nspkg 在【用户文档区】(user-data) 中的相对路径。
     *
     * # 交接目录是 user-data，不是调用方的私有目录
     *
     * 此前这里写的是"调用方私有数据目录"，而那是错的：每个包的私有目录是 0700、
     * 属主为该包 UID，Provider 读不进去。于是协议与实现指的是两个不同的地方，
     * 【任何 App 发起的安装都不可能成功】——调用方能写的位置 Provider 读不到，
     * Provider 能读的位置调用方写不进。
     *
     * 用 user-data 而不是新造一个专用交接目录：它已经是"跨包共享的用户文件"
     * 那块地，用户下载或拷进来的 .nspkg 本来就落在这里，文件管理器也在这里。
     * 再造一个只会多出一次纯粹的复制，换不来任何隔离（两者都全系统可读）。
     *
     * 【刻意不是绝对路径】。绝对路径等于让调用方指定文件系统上任意位置，而
     * Provider 跑在 ProtectSystem=strict 的沙箱里，一个它读不到的绝对路径只会
     * 得到含义模糊的"打不开"。相对路径把交接位置写进协议本身。
     *
     * 必须是不含 ".." 的相对路径；Provider 与 nervud 都会再校验一次。
     * </pre>
     *
     * <code>string nspkg_relpath = 1 [json_name = "nspkgRelpath"];</code>
     * @return This builder for chaining.
     */
    public Builder clearNspkgRelpath() {
      nspkgRelpath_ = getDefaultInstance().getNspkgRelpath();
      bitField0_ = (bitField0_ & ~0x00000001);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * .nspkg 在【用户文档区】(user-data) 中的相对路径。
     *
     * # 交接目录是 user-data，不是调用方的私有目录
     *
     * 此前这里写的是"调用方私有数据目录"，而那是错的：每个包的私有目录是 0700、
     * 属主为该包 UID，Provider 读不进去。于是协议与实现指的是两个不同的地方，
     * 【任何 App 发起的安装都不可能成功】——调用方能写的位置 Provider 读不到，
     * Provider 能读的位置调用方写不进。
     *
     * 用 user-data 而不是新造一个专用交接目录：它已经是"跨包共享的用户文件"
     * 那块地，用户下载或拷进来的 .nspkg 本来就落在这里，文件管理器也在这里。
     * 再造一个只会多出一次纯粹的复制，换不来任何隔离（两者都全系统可读）。
     *
     * 【刻意不是绝对路径】。绝对路径等于让调用方指定文件系统上任意位置，而
     * Provider 跑在 ProtectSystem=strict 的沙箱里，一个它读不到的绝对路径只会
     * 得到含义模糊的"打不开"。相对路径把交接位置写进协议本身。
     *
     * 必须是不含 ".." 的相对路径；Provider 与 nervud 都会再校验一次。
     * </pre>
     *
     * <code>string nspkg_relpath = 1 [json_name = "nspkgRelpath"];</code>
     * @param value The bytes for nspkgRelpath to set.
     * @return This builder for chaining.
     */
    public Builder setNspkgRelpathBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      nspkgRelpath_ = value;
      bitField0_ |= 0x00000001;
      onChanged();
      return this;
    }

    private com.google.protobuf.LazyStringArrayList consentedPermissions_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
    private void ensureConsentedPermissionsIsMutable() {
      if (!consentedPermissions_.isModifiable()) {
        consentedPermissions_ = new com.google.protobuf.LazyStringArrayList(consentedPermissions_);
      }
      bitField0_ |= 0x00000002;
    }
    /**
     * <pre>
     * 用户在安装确认屏上同意的敏感权限 ID 列表。
     *
     * # 它解决的是什么
     *
     * USER_CONSENT 权限（perm.camera.capture 一类）在安装期能进
     * GrantedPermissions，但运行期 AllowedAt 要求 GrantState == GRANTED，默认是
     * NOT_REQUESTED。也就是说一个普通应用装上之后，它申请的敏感权限【永远】拿
     * 不到——应用无法自救（SDK 没有请求权限的 API），用户也没有界面可批。本字段
     * 让安装动作把这一问补上：装之前先把该包申请的敏感权限摊给用户看，用户同意
     * 哪些，安装成功后那些权限的运行期状态就直接落成 GRANTED。
     *
     * # 谁有资格填它
     *
     * 【只有系统的确认界面】。经 IPC 装包必须穿过 nervus.interface.permission.ui
     * 的 ConfirmInstall：INSTALL 的 needs_user_confirmation 是 true，而 nervud 的
     * 那道门只对持有 perm.permission.admin 的调用方放行，全系统只有
     * nervus.permissionui 持有它。因此普通应用填不进来——它连 INSTALL 都调不到。
     *
     * # 内核还会再过滤一遍
     *
     * nervud 只对【同意清单 ∩ 安装期授予集合 ∩ USER_CONSENT】三者的交集落库，
     * 越界的条目静默忽略：一条没申请过的、或不是 USER_CONSENT 的权限出现在这里，
     * 不该因为确认界面写错就变成一次真实授予。因此本字段是「用户同意了什么」的
     * 陈述，不是「请授予什么」的命令。
     * </pre>
     *
     * <code>repeated string consented_permissions = 2 [json_name = "consentedPermissions"];</code>
     * @return A list containing the consentedPermissions.
     */
    public com.google.protobuf.ProtocolStringList
        getConsentedPermissionsList() {
      consentedPermissions_.makeImmutable();
      return consentedPermissions_;
    }
    /**
     * <pre>
     * 用户在安装确认屏上同意的敏感权限 ID 列表。
     *
     * # 它解决的是什么
     *
     * USER_CONSENT 权限（perm.camera.capture 一类）在安装期能进
     * GrantedPermissions，但运行期 AllowedAt 要求 GrantState == GRANTED，默认是
     * NOT_REQUESTED。也就是说一个普通应用装上之后，它申请的敏感权限【永远】拿
     * 不到——应用无法自救（SDK 没有请求权限的 API），用户也没有界面可批。本字段
     * 让安装动作把这一问补上：装之前先把该包申请的敏感权限摊给用户看，用户同意
     * 哪些，安装成功后那些权限的运行期状态就直接落成 GRANTED。
     *
     * # 谁有资格填它
     *
     * 【只有系统的确认界面】。经 IPC 装包必须穿过 nervus.interface.permission.ui
     * 的 ConfirmInstall：INSTALL 的 needs_user_confirmation 是 true，而 nervud 的
     * 那道门只对持有 perm.permission.admin 的调用方放行，全系统只有
     * nervus.permissionui 持有它。因此普通应用填不进来——它连 INSTALL 都调不到。
     *
     * # 内核还会再过滤一遍
     *
     * nervud 只对【同意清单 ∩ 安装期授予集合 ∩ USER_CONSENT】三者的交集落库，
     * 越界的条目静默忽略：一条没申请过的、或不是 USER_CONSENT 的权限出现在这里，
     * 不该因为确认界面写错就变成一次真实授予。因此本字段是「用户同意了什么」的
     * 陈述，不是「请授予什么」的命令。
     * </pre>
     *
     * <code>repeated string consented_permissions = 2 [json_name = "consentedPermissions"];</code>
     * @return The count of consentedPermissions.
     */
    public int getConsentedPermissionsCount() {
      return consentedPermissions_.size();
    }
    /**
     * <pre>
     * 用户在安装确认屏上同意的敏感权限 ID 列表。
     *
     * # 它解决的是什么
     *
     * USER_CONSENT 权限（perm.camera.capture 一类）在安装期能进
     * GrantedPermissions，但运行期 AllowedAt 要求 GrantState == GRANTED，默认是
     * NOT_REQUESTED。也就是说一个普通应用装上之后，它申请的敏感权限【永远】拿
     * 不到——应用无法自救（SDK 没有请求权限的 API），用户也没有界面可批。本字段
     * 让安装动作把这一问补上：装之前先把该包申请的敏感权限摊给用户看，用户同意
     * 哪些，安装成功后那些权限的运行期状态就直接落成 GRANTED。
     *
     * # 谁有资格填它
     *
     * 【只有系统的确认界面】。经 IPC 装包必须穿过 nervus.interface.permission.ui
     * 的 ConfirmInstall：INSTALL 的 needs_user_confirmation 是 true，而 nervud 的
     * 那道门只对持有 perm.permission.admin 的调用方放行，全系统只有
     * nervus.permissionui 持有它。因此普通应用填不进来——它连 INSTALL 都调不到。
     *
     * # 内核还会再过滤一遍
     *
     * nervud 只对【同意清单 ∩ 安装期授予集合 ∩ USER_CONSENT】三者的交集落库，
     * 越界的条目静默忽略：一条没申请过的、或不是 USER_CONSENT 的权限出现在这里，
     * 不该因为确认界面写错就变成一次真实授予。因此本字段是「用户同意了什么」的
     * 陈述，不是「请授予什么」的命令。
     * </pre>
     *
     * <code>repeated string consented_permissions = 2 [json_name = "consentedPermissions"];</code>
     * @param index The index of the element to return.
     * @return The consentedPermissions at the given index.
     */
    public java.lang.String getConsentedPermissions(int index) {
      return consentedPermissions_.get(index);
    }
    /**
     * <pre>
     * 用户在安装确认屏上同意的敏感权限 ID 列表。
     *
     * # 它解决的是什么
     *
     * USER_CONSENT 权限（perm.camera.capture 一类）在安装期能进
     * GrantedPermissions，但运行期 AllowedAt 要求 GrantState == GRANTED，默认是
     * NOT_REQUESTED。也就是说一个普通应用装上之后，它申请的敏感权限【永远】拿
     * 不到——应用无法自救（SDK 没有请求权限的 API），用户也没有界面可批。本字段
     * 让安装动作把这一问补上：装之前先把该包申请的敏感权限摊给用户看，用户同意
     * 哪些，安装成功后那些权限的运行期状态就直接落成 GRANTED。
     *
     * # 谁有资格填它
     *
     * 【只有系统的确认界面】。经 IPC 装包必须穿过 nervus.interface.permission.ui
     * 的 ConfirmInstall：INSTALL 的 needs_user_confirmation 是 true，而 nervud 的
     * 那道门只对持有 perm.permission.admin 的调用方放行，全系统只有
     * nervus.permissionui 持有它。因此普通应用填不进来——它连 INSTALL 都调不到。
     *
     * # 内核还会再过滤一遍
     *
     * nervud 只对【同意清单 ∩ 安装期授予集合 ∩ USER_CONSENT】三者的交集落库，
     * 越界的条目静默忽略：一条没申请过的、或不是 USER_CONSENT 的权限出现在这里，
     * 不该因为确认界面写错就变成一次真实授予。因此本字段是「用户同意了什么」的
     * 陈述，不是「请授予什么」的命令。
     * </pre>
     *
     * <code>repeated string consented_permissions = 2 [json_name = "consentedPermissions"];</code>
     * @param index The index of the value to return.
     * @return The bytes of the consentedPermissions at the given index.
     */
    public com.google.protobuf.ByteString
        getConsentedPermissionsBytes(int index) {
      return consentedPermissions_.getByteString(index);
    }
    /**
     * <pre>
     * 用户在安装确认屏上同意的敏感权限 ID 列表。
     *
     * # 它解决的是什么
     *
     * USER_CONSENT 权限（perm.camera.capture 一类）在安装期能进
     * GrantedPermissions，但运行期 AllowedAt 要求 GrantState == GRANTED，默认是
     * NOT_REQUESTED。也就是说一个普通应用装上之后，它申请的敏感权限【永远】拿
     * 不到——应用无法自救（SDK 没有请求权限的 API），用户也没有界面可批。本字段
     * 让安装动作把这一问补上：装之前先把该包申请的敏感权限摊给用户看，用户同意
     * 哪些，安装成功后那些权限的运行期状态就直接落成 GRANTED。
     *
     * # 谁有资格填它
     *
     * 【只有系统的确认界面】。经 IPC 装包必须穿过 nervus.interface.permission.ui
     * 的 ConfirmInstall：INSTALL 的 needs_user_confirmation 是 true，而 nervud 的
     * 那道门只对持有 perm.permission.admin 的调用方放行，全系统只有
     * nervus.permissionui 持有它。因此普通应用填不进来——它连 INSTALL 都调不到。
     *
     * # 内核还会再过滤一遍
     *
     * nervud 只对【同意清单 ∩ 安装期授予集合 ∩ USER_CONSENT】三者的交集落库，
     * 越界的条目静默忽略：一条没申请过的、或不是 USER_CONSENT 的权限出现在这里，
     * 不该因为确认界面写错就变成一次真实授予。因此本字段是「用户同意了什么」的
     * 陈述，不是「请授予什么」的命令。
     * </pre>
     *
     * <code>repeated string consented_permissions = 2 [json_name = "consentedPermissions"];</code>
     * @param index The index to set the value at.
     * @param value The consentedPermissions to set.
     * @return This builder for chaining.
     */
    public Builder setConsentedPermissions(
        int index, java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      ensureConsentedPermissionsIsMutable();
      consentedPermissions_.set(index, value);
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * 用户在安装确认屏上同意的敏感权限 ID 列表。
     *
     * # 它解决的是什么
     *
     * USER_CONSENT 权限（perm.camera.capture 一类）在安装期能进
     * GrantedPermissions，但运行期 AllowedAt 要求 GrantState == GRANTED，默认是
     * NOT_REQUESTED。也就是说一个普通应用装上之后，它申请的敏感权限【永远】拿
     * 不到——应用无法自救（SDK 没有请求权限的 API），用户也没有界面可批。本字段
     * 让安装动作把这一问补上：装之前先把该包申请的敏感权限摊给用户看，用户同意
     * 哪些，安装成功后那些权限的运行期状态就直接落成 GRANTED。
     *
     * # 谁有资格填它
     *
     * 【只有系统的确认界面】。经 IPC 装包必须穿过 nervus.interface.permission.ui
     * 的 ConfirmInstall：INSTALL 的 needs_user_confirmation 是 true，而 nervud 的
     * 那道门只对持有 perm.permission.admin 的调用方放行，全系统只有
     * nervus.permissionui 持有它。因此普通应用填不进来——它连 INSTALL 都调不到。
     *
     * # 内核还会再过滤一遍
     *
     * nervud 只对【同意清单 ∩ 安装期授予集合 ∩ USER_CONSENT】三者的交集落库，
     * 越界的条目静默忽略：一条没申请过的、或不是 USER_CONSENT 的权限出现在这里，
     * 不该因为确认界面写错就变成一次真实授予。因此本字段是「用户同意了什么」的
     * 陈述，不是「请授予什么」的命令。
     * </pre>
     *
     * <code>repeated string consented_permissions = 2 [json_name = "consentedPermissions"];</code>
     * @param value The consentedPermissions to add.
     * @return This builder for chaining.
     */
    public Builder addConsentedPermissions(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      ensureConsentedPermissionsIsMutable();
      consentedPermissions_.add(value);
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * 用户在安装确认屏上同意的敏感权限 ID 列表。
     *
     * # 它解决的是什么
     *
     * USER_CONSENT 权限（perm.camera.capture 一类）在安装期能进
     * GrantedPermissions，但运行期 AllowedAt 要求 GrantState == GRANTED，默认是
     * NOT_REQUESTED。也就是说一个普通应用装上之后，它申请的敏感权限【永远】拿
     * 不到——应用无法自救（SDK 没有请求权限的 API），用户也没有界面可批。本字段
     * 让安装动作把这一问补上：装之前先把该包申请的敏感权限摊给用户看，用户同意
     * 哪些，安装成功后那些权限的运行期状态就直接落成 GRANTED。
     *
     * # 谁有资格填它
     *
     * 【只有系统的确认界面】。经 IPC 装包必须穿过 nervus.interface.permission.ui
     * 的 ConfirmInstall：INSTALL 的 needs_user_confirmation 是 true，而 nervud 的
     * 那道门只对持有 perm.permission.admin 的调用方放行，全系统只有
     * nervus.permissionui 持有它。因此普通应用填不进来——它连 INSTALL 都调不到。
     *
     * # 内核还会再过滤一遍
     *
     * nervud 只对【同意清单 ∩ 安装期授予集合 ∩ USER_CONSENT】三者的交集落库，
     * 越界的条目静默忽略：一条没申请过的、或不是 USER_CONSENT 的权限出现在这里，
     * 不该因为确认界面写错就变成一次真实授予。因此本字段是「用户同意了什么」的
     * 陈述，不是「请授予什么」的命令。
     * </pre>
     *
     * <code>repeated string consented_permissions = 2 [json_name = "consentedPermissions"];</code>
     * @param values The consentedPermissions to add.
     * @return This builder for chaining.
     */
    public Builder addAllConsentedPermissions(
        java.lang.Iterable<java.lang.String> values) {
      ensureConsentedPermissionsIsMutable();
      com.google.protobuf.AbstractMessageLite.Builder.addAll(
          values, consentedPermissions_);
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * 用户在安装确认屏上同意的敏感权限 ID 列表。
     *
     * # 它解决的是什么
     *
     * USER_CONSENT 权限（perm.camera.capture 一类）在安装期能进
     * GrantedPermissions，但运行期 AllowedAt 要求 GrantState == GRANTED，默认是
     * NOT_REQUESTED。也就是说一个普通应用装上之后，它申请的敏感权限【永远】拿
     * 不到——应用无法自救（SDK 没有请求权限的 API），用户也没有界面可批。本字段
     * 让安装动作把这一问补上：装之前先把该包申请的敏感权限摊给用户看，用户同意
     * 哪些，安装成功后那些权限的运行期状态就直接落成 GRANTED。
     *
     * # 谁有资格填它
     *
     * 【只有系统的确认界面】。经 IPC 装包必须穿过 nervus.interface.permission.ui
     * 的 ConfirmInstall：INSTALL 的 needs_user_confirmation 是 true，而 nervud 的
     * 那道门只对持有 perm.permission.admin 的调用方放行，全系统只有
     * nervus.permissionui 持有它。因此普通应用填不进来——它连 INSTALL 都调不到。
     *
     * # 内核还会再过滤一遍
     *
     * nervud 只对【同意清单 ∩ 安装期授予集合 ∩ USER_CONSENT】三者的交集落库，
     * 越界的条目静默忽略：一条没申请过的、或不是 USER_CONSENT 的权限出现在这里，
     * 不该因为确认界面写错就变成一次真实授予。因此本字段是「用户同意了什么」的
     * 陈述，不是「请授予什么」的命令。
     * </pre>
     *
     * <code>repeated string consented_permissions = 2 [json_name = "consentedPermissions"];</code>
     * @return This builder for chaining.
     */
    public Builder clearConsentedPermissions() {
      consentedPermissions_ =
        com.google.protobuf.LazyStringArrayList.emptyList();
      bitField0_ = (bitField0_ & ~0x00000002);;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * 用户在安装确认屏上同意的敏感权限 ID 列表。
     *
     * # 它解决的是什么
     *
     * USER_CONSENT 权限（perm.camera.capture 一类）在安装期能进
     * GrantedPermissions，但运行期 AllowedAt 要求 GrantState == GRANTED，默认是
     * NOT_REQUESTED。也就是说一个普通应用装上之后，它申请的敏感权限【永远】拿
     * 不到——应用无法自救（SDK 没有请求权限的 API），用户也没有界面可批。本字段
     * 让安装动作把这一问补上：装之前先把该包申请的敏感权限摊给用户看，用户同意
     * 哪些，安装成功后那些权限的运行期状态就直接落成 GRANTED。
     *
     * # 谁有资格填它
     *
     * 【只有系统的确认界面】。经 IPC 装包必须穿过 nervus.interface.permission.ui
     * 的 ConfirmInstall：INSTALL 的 needs_user_confirmation 是 true，而 nervud 的
     * 那道门只对持有 perm.permission.admin 的调用方放行，全系统只有
     * nervus.permissionui 持有它。因此普通应用填不进来——它连 INSTALL 都调不到。
     *
     * # 内核还会再过滤一遍
     *
     * nervud 只对【同意清单 ∩ 安装期授予集合 ∩ USER_CONSENT】三者的交集落库，
     * 越界的条目静默忽略：一条没申请过的、或不是 USER_CONSENT 的权限出现在这里，
     * 不该因为确认界面写错就变成一次真实授予。因此本字段是「用户同意了什么」的
     * 陈述，不是「请授予什么」的命令。
     * </pre>
     *
     * <code>repeated string consented_permissions = 2 [json_name = "consentedPermissions"];</code>
     * @param value The bytes of the consentedPermissions to add.
     * @return This builder for chaining.
     */
    public Builder addConsentedPermissionsBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      ensureConsentedPermissionsIsMutable();
      consentedPermissions_.add(value);
      bitField0_ |= 0x00000002;
      onChanged();
      return this;
    }

    private java.lang.Object expectedManifestDigest_ = "";
    /**
     * <pre>
     * 确认屏那次 INSPECT 回的 InspectResult.manifest_digest，原样回传。
     *
     * # 它关的是"看的是 A、装的是 B"
     *
     * .nspkg 放在跨包共享的 user-data 里，因此调用方在 INSPECT 与 INSTALL
     * 【之间】完全可以把文件换掉。两次调用各自都合法，单看任何一次都发现不了
     * ——而用户是基于 A 的权限清单点的头，consented_permissions 却会被用到 B 上。
     *
     * 带上它，内核会比对；不符即拒（PACKAGE_MANAGER_REASON_CONTENT_CHANGED）。
     *
     * 【为空表示不校验】。必须允许为空：系统装机脚本与 deploy 直接装包，从来没有
     * INSPECT 那一步，强制要求会把它们全部打断。凡是先给用户看过权限清单的
     * 调用方都【必须】带上它，否则那次确认没有约束力。
     * </pre>
     *
     * <code>string expected_manifest_digest = 3 [json_name = "expectedManifestDigest"];</code>
     * @return The expectedManifestDigest.
     */
    public java.lang.String getExpectedManifestDigest() {
      java.lang.Object ref = expectedManifestDigest_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs =
            (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        expectedManifestDigest_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     * <pre>
     * 确认屏那次 INSPECT 回的 InspectResult.manifest_digest，原样回传。
     *
     * # 它关的是"看的是 A、装的是 B"
     *
     * .nspkg 放在跨包共享的 user-data 里，因此调用方在 INSPECT 与 INSTALL
     * 【之间】完全可以把文件换掉。两次调用各自都合法，单看任何一次都发现不了
     * ——而用户是基于 A 的权限清单点的头，consented_permissions 却会被用到 B 上。
     *
     * 带上它，内核会比对；不符即拒（PACKAGE_MANAGER_REASON_CONTENT_CHANGED）。
     *
     * 【为空表示不校验】。必须允许为空：系统装机脚本与 deploy 直接装包，从来没有
     * INSPECT 那一步，强制要求会把它们全部打断。凡是先给用户看过权限清单的
     * 调用方都【必须】带上它，否则那次确认没有约束力。
     * </pre>
     *
     * <code>string expected_manifest_digest = 3 [json_name = "expectedManifestDigest"];</code>
     * @return The bytes for expectedManifestDigest.
     */
    public com.google.protobuf.ByteString
        getExpectedManifestDigestBytes() {
      java.lang.Object ref = expectedManifestDigest_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b = 
            com.google.protobuf.ByteString.copyFromUtf8(
                (java.lang.String) ref);
        expectedManifestDigest_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     * <pre>
     * 确认屏那次 INSPECT 回的 InspectResult.manifest_digest，原样回传。
     *
     * # 它关的是"看的是 A、装的是 B"
     *
     * .nspkg 放在跨包共享的 user-data 里，因此调用方在 INSPECT 与 INSTALL
     * 【之间】完全可以把文件换掉。两次调用各自都合法，单看任何一次都发现不了
     * ——而用户是基于 A 的权限清单点的头，consented_permissions 却会被用到 B 上。
     *
     * 带上它，内核会比对；不符即拒（PACKAGE_MANAGER_REASON_CONTENT_CHANGED）。
     *
     * 【为空表示不校验】。必须允许为空：系统装机脚本与 deploy 直接装包，从来没有
     * INSPECT 那一步，强制要求会把它们全部打断。凡是先给用户看过权限清单的
     * 调用方都【必须】带上它，否则那次确认没有约束力。
     * </pre>
     *
     * <code>string expected_manifest_digest = 3 [json_name = "expectedManifestDigest"];</code>
     * @param value The expectedManifestDigest to set.
     * @return This builder for chaining.
     */
    public Builder setExpectedManifestDigest(
        java.lang.String value) {
      if (value == null) { throw new NullPointerException(); }
      expectedManifestDigest_ = value;
      bitField0_ |= 0x00000004;
      onChanged();
      return this;
    }
    /**
     * <pre>
     * 确认屏那次 INSPECT 回的 InspectResult.manifest_digest，原样回传。
     *
     * # 它关的是"看的是 A、装的是 B"
     *
     * .nspkg 放在跨包共享的 user-data 里，因此调用方在 INSPECT 与 INSTALL
     * 【之间】完全可以把文件换掉。两次调用各自都合法，单看任何一次都发现不了
     * ——而用户是基于 A 的权限清单点的头，consented_permissions 却会被用到 B 上。
     *
     * 带上它，内核会比对；不符即拒（PACKAGE_MANAGER_REASON_CONTENT_CHANGED）。
     *
     * 【为空表示不校验】。必须允许为空：系统装机脚本与 deploy 直接装包，从来没有
     * INSPECT 那一步，强制要求会把它们全部打断。凡是先给用户看过权限清单的
     * 调用方都【必须】带上它，否则那次确认没有约束力。
     * </pre>
     *
     * <code>string expected_manifest_digest = 3 [json_name = "expectedManifestDigest"];</code>
     * @return This builder for chaining.
     */
    public Builder clearExpectedManifestDigest() {
      expectedManifestDigest_ = getDefaultInstance().getExpectedManifestDigest();
      bitField0_ = (bitField0_ & ~0x00000004);
      onChanged();
      return this;
    }
    /**
     * <pre>
     * 确认屏那次 INSPECT 回的 InspectResult.manifest_digest，原样回传。
     *
     * # 它关的是"看的是 A、装的是 B"
     *
     * .nspkg 放在跨包共享的 user-data 里，因此调用方在 INSPECT 与 INSTALL
     * 【之间】完全可以把文件换掉。两次调用各自都合法，单看任何一次都发现不了
     * ——而用户是基于 A 的权限清单点的头，consented_permissions 却会被用到 B 上。
     *
     * 带上它，内核会比对；不符即拒（PACKAGE_MANAGER_REASON_CONTENT_CHANGED）。
     *
     * 【为空表示不校验】。必须允许为空：系统装机脚本与 deploy 直接装包，从来没有
     * INSPECT 那一步，强制要求会把它们全部打断。凡是先给用户看过权限清单的
     * 调用方都【必须】带上它，否则那次确认没有约束力。
     * </pre>
     *
     * <code>string expected_manifest_digest = 3 [json_name = "expectedManifestDigest"];</code>
     * @param value The bytes for expectedManifestDigest to set.
     * @return This builder for chaining.
     */
    public Builder setExpectedManifestDigestBytes(
        com.google.protobuf.ByteString value) {
      if (value == null) { throw new NullPointerException(); }
      checkByteStringIsUtf8(value);
      expectedManifestDigest_ = value;
      bitField0_ |= 0x00000004;
      onChanged();
      return this;
    }

    // @@protoc_insertion_point(builder_scope:nervus.interface.pkgmanager.v1.InstallRequest)
  }

  // @@protoc_insertion_point(class_scope:nervus.interface.pkgmanager.v1.InstallRequest)
  private static final io.github.nervusos.iface.pkgmanager.v1.InstallRequest DEFAULT_INSTANCE;
  static {
    DEFAULT_INSTANCE = new io.github.nervusos.iface.pkgmanager.v1.InstallRequest();
  }

  public static io.github.nervusos.iface.pkgmanager.v1.InstallRequest getDefaultInstance() {
    return DEFAULT_INSTANCE;
  }

  private static final com.google.protobuf.Parser<InstallRequest>
      PARSER = new com.google.protobuf.AbstractParser<InstallRequest>() {
    @java.lang.Override
    public InstallRequest parsePartialFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws com.google.protobuf.InvalidProtocolBufferException {
      Builder builder = newBuilder();
      try {
        builder.mergeFrom(input, extensionRegistry);
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        throw e.setUnfinishedMessage(builder.buildPartial());
      } catch (com.google.protobuf.UninitializedMessageException e) {
        throw e.asInvalidProtocolBufferException().setUnfinishedMessage(builder.buildPartial());
      } catch (java.io.IOException e) {
        throw new com.google.protobuf.InvalidProtocolBufferException(e)
            .setUnfinishedMessage(builder.buildPartial());
      }
      return builder.buildPartial();
    }
  };

  public static com.google.protobuf.Parser<InstallRequest> parser() {
    return PARSER;
  }

  @java.lang.Override
  public com.google.protobuf.Parser<InstallRequest> getParserForType() {
    return PARSER;
  }

  @java.lang.Override
  public io.github.nervusos.iface.pkgmanager.v1.InstallRequest getDefaultInstanceForType() {
    return DEFAULT_INSTANCE;
  }

}

