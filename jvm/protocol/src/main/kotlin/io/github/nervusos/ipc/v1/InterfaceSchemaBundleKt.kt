// Generated by the protocol buffer compiler. DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: nervus/ipc/v1/schema.proto

// Generated files should ignore deprecation warnings
@file:Suppress("DEPRECATION")
package io.github.nervusos.ipc.v1;

@kotlin.jvm.JvmName("-initializeinterfaceSchemaBundle")
public inline fun interfaceSchemaBundle(block: io.github.nervusos.ipc.v1.InterfaceSchemaBundleKt.Dsl.() -> kotlin.Unit): io.github.nervusos.ipc.v1.InterfaceSchemaBundle =
  io.github.nervusos.ipc.v1.InterfaceSchemaBundleKt.Dsl._create(io.github.nervusos.ipc.v1.InterfaceSchemaBundle.newBuilder()).apply { block() }._build()
/**
 * ```
 * InterfaceSchemaBundle 是【单个】Interface 的类型信息包。
 *
 * 它携带一份自包含的 FileDescriptorSet（含该接口 Request/Response/event/
 * error_detail 消息的完整描述符及其传递依赖），加上把它钉死到目录投影的
 * interface_id / version / schema_hash 三元组。
 * ```
 *
 * Protobuf type `nervus.ipc.v1.InterfaceSchemaBundle`
 */
public object InterfaceSchemaBundleKt {
  @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
  @com.google.protobuf.kotlin.ProtoDslMarker
  public class Dsl private constructor(
    private val _builder: io.github.nervusos.ipc.v1.InterfaceSchemaBundle.Builder
  ) {
    public companion object {
      @kotlin.jvm.JvmSynthetic
    @kotlin.PublishedApi
      internal fun _create(builder: io.github.nervusos.ipc.v1.InterfaceSchemaBundle.Builder): Dsl = Dsl(builder)
    }

    @kotlin.jvm.JvmSynthetic
  @kotlin.PublishedApi
    internal fun _build(): io.github.nervusos.ipc.v1.InterfaceSchemaBundle = _builder.build()

    /**
     * ```
     * 逻辑接口 ID，如 "nervus.interface.motion.base" 或 OEM 私有的
     * "com.acme.interface.gripper"。与 ResolveEndpoint.interface_id 同一命名空间。
     * ```
     *
     * `string interface_id = 1 [json_name = "interfaceId"];`
     */
    public var interfaceId: kotlin.String
      @JvmName("getInterfaceId")
      get() = _builder.interfaceId
      @JvmName("setInterfaceId")
      set(value) {
        _builder.interfaceId = value
      }
    /**
     * ```
     * 逻辑接口 ID，如 "nervus.interface.motion.base" 或 OEM 私有的
     * "com.acme.interface.gripper"。与 ResolveEndpoint.interface_id 同一命名空间。
     * ```
     *
     * `string interface_id = 1 [json_name = "interfaceId"];`
     */
    public fun clearInterfaceId() {
      _builder.clearInterfaceId()
    }

    /**
     * ```
     * 接口 major 版本。与 ResolveEndpointSuccess.interface_major 对齐。
     * 用 major 而非 major.minor：minor 只增不减、向后兼容，同一 bundle 覆盖
     * 一个 major 下的全部 minor；跨 major 是不同 bundle。
     * ```
     *
     * `uint32 version = 2 [json_name = "version"];`
     */
    public var version: kotlin.Int
      @JvmName("getVersion")
      get() = _builder.version
      @JvmName("setVersion")
      set(value) {
        _builder.version = value
      }
    /**
     * ```
     * 接口 major 版本。与 ResolveEndpointSuccess.interface_major 对齐。
     * 用 major 而非 major.minor：minor 只增不减、向后兼容，同一 bundle 覆盖
     * 一个 major 下的全部 minor；跨 major 是不同 bundle。
     * ```
     *
     * `uint32 version = 2 [json_name = "version"];`
     */
    public fun clearVersion() {
      _builder.clearVersion()
    }

    /**
     * ```
     * 本 bundle 内容的确定性标识（对 file_descriptor_set 的规范化哈希）。
     *
     * 这是唯一的绑定锚点：安装时 nervud 计算并登记它；RobotCatalog /
     * ResolveEndpoint 投影回显它；SDK 校验本地解码用的 bundle 与投影一致。
     * 不一致即 schema 漂移，必须在第一次调用前暴露，而不是让某个字段被静默丢弃。
     * 具体哈希算法由 nervud 实现约定（不在 wire 上自述，避免"选择校验器"攻击）。
     * ```
     *
     * `bytes schema_hash = 3 [json_name = "schemaHash"];`
     */
    public var schemaHash: com.google.protobuf.ByteString
      @JvmName("getSchemaHash")
      get() = _builder.schemaHash
      @JvmName("setSchemaHash")
      set(value) {
        _builder.schemaHash = value
      }
    /**
     * ```
     * 本 bundle 内容的确定性标识（对 file_descriptor_set 的规范化哈希）。
     *
     * 这是唯一的绑定锚点：安装时 nervud 计算并登记它；RobotCatalog /
     * ResolveEndpoint 投影回显它；SDK 校验本地解码用的 bundle 与投影一致。
     * 不一致即 schema 漂移，必须在第一次调用前暴露，而不是让某个字段被静默丢弃。
     * 具体哈希算法由 nervud 实现约定（不在 wire 上自述，避免"选择校验器"攻击）。
     * ```
     *
     * `bytes schema_hash = 3 [json_name = "schemaHash"];`
     */
    public fun clearSchemaHash() {
      _builder.clearSchemaHash()
    }

    /**
     * ```
     * 序列化后的 google.protobuf.FileDescriptorSet。
     *
     * 【刻意用 bytes 而非直接 import descriptor.proto 的类型】：本仓库要在三种语言
     * 里生成，且 bundle 常常只是被存储 / 转发 / 计算 hash（安装器、Catalog 投影），
     * 而不是当场反射解析。用 bytes 让这些环节无需链接 protobuf 的 descriptor 运行时；
     * 真正需要反射解码的一侧（SDK / Agent）再自行 parse 成 FileDescriptorSet。
     * 这也让 schema_hash 的计算对象是一段明确的字节串，跨语言无歧义。
     * ```
     *
     * `bytes file_descriptor_set = 4 [json_name = "fileDescriptorSet"];`
     */
    public var fileDescriptorSet: com.google.protobuf.ByteString
      @JvmName("getFileDescriptorSet")
      get() = _builder.fileDescriptorSet
      @JvmName("setFileDescriptorSet")
      set(value) {
        _builder.fileDescriptorSet = value
      }
    /**
     * ```
     * 序列化后的 google.protobuf.FileDescriptorSet。
     *
     * 【刻意用 bytes 而非直接 import descriptor.proto 的类型】：本仓库要在三种语言
     * 里生成，且 bundle 常常只是被存储 / 转发 / 计算 hash（安装器、Catalog 投影），
     * 而不是当场反射解析。用 bytes 让这些环节无需链接 protobuf 的 descriptor 运行时；
     * 真正需要反射解码的一侧（SDK / Agent）再自行 parse 成 FileDescriptorSet。
     * 这也让 schema_hash 的计算对象是一段明确的字节串，跨语言无歧义。
     * ```
     *
     * `bytes file_descriptor_set = 4 [json_name = "fileDescriptorSet"];`
     */
    public fun clearFileDescriptorSet() {
      _builder.clearFileDescriptorSet()
    }
  }
}
@kotlin.jvm.JvmSynthetic
public inline fun io.github.nervusos.ipc.v1.InterfaceSchemaBundle.copy(block: `io.github.nervusos.ipc.v1`.InterfaceSchemaBundleKt.Dsl.() -> kotlin.Unit): io.github.nervusos.ipc.v1.InterfaceSchemaBundle =
  `io.github.nervusos.ipc.v1`.InterfaceSchemaBundleKt.Dsl._create(this.toBuilder()).apply { block() }._build()

