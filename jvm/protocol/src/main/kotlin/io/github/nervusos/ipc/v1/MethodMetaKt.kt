// Generated by the protocol buffer compiler. DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: nervus/ipc/v1/method_registry.proto

// Generated files should ignore deprecation warnings
@file:Suppress("DEPRECATION")
package io.github.nervusos.ipc.v1;

@kotlin.jvm.JvmName("-initializemethodMeta")
public inline fun methodMeta(block: io.github.nervusos.ipc.v1.MethodMetaKt.Dsl.() -> kotlin.Unit): io.github.nervusos.ipc.v1.MethodMeta =
  io.github.nervusos.ipc.v1.MethodMetaKt.Dsl._create(io.github.nervusos.ipc.v1.MethodMeta.newBuilder()).apply { block() }._build()
/**
 * ```
 * MethodMeta 是挂在【某个接口的 method_id 枚举值】上的元数据。
 *
 * 真相源永远是 proto：Go / SDK / Agent 只从生成的 EnumValueOptions 里读它，
 * 不在别处另写清单。字段一经发布遵循 §10.12 兼容规则（号不复用、删除写
 * reserved、新增字段须有安全默认）。
 * ```
 *
 * Protobuf type `nervus.ipc.v1.MethodMeta`
 */
public object MethodMetaKt {
  @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
  @com.google.protobuf.kotlin.ProtoDslMarker
  public class Dsl private constructor(
    private val _builder: io.github.nervusos.ipc.v1.MethodMeta.Builder
  ) {
    public companion object {
      @kotlin.jvm.JvmSynthetic
    @kotlin.PublishedApi
      internal fun _create(builder: io.github.nervusos.ipc.v1.MethodMeta.Builder): Dsl = Dsl(builder)
    }

    @kotlin.jvm.JvmSynthetic
  @kotlin.PublishedApi
    internal fun _build(): io.github.nervusos.ipc.v1.MethodMeta = _builder.build()

    /**
     * ```
     * 稳定的数字 method_id，进 Request.method_id / Dispatch.method_id。
     *
     * 【必须等于所在枚举值的数字值】——枚举值本身承载 wire 上的 method_id，
     * 这里冗余一份是为了让「只拿到一条 MethodMeta」的消费者也自带 id，不必
     * 回查枚举。抽取工具会校验两者一致，不一致即 schema 错误。
     * ```
     *
     * `uint32 method_id = 1 [json_name = "methodId"];`
     */
    public var methodId: kotlin.Int
      @JvmName("getMethodId")
      get() = _builder.methodId
      @JvmName("setMethodId")
      set(value) {
        _builder.methodId = value
      }
    /**
     * ```
     * 稳定的数字 method_id，进 Request.method_id / Dispatch.method_id。
     *
     * 【必须等于所在枚举值的数字值】——枚举值本身承载 wire 上的 method_id，
     * 这里冗余一份是为了让「只拿到一条 MethodMeta」的消费者也自带 id，不必
     * 回查枚举。抽取工具会校验两者一致，不一致即 schema 错误。
     * ```
     *
     * `uint32 method_id = 1 [json_name = "methodId"];`
     */
    public fun clearMethodId() {
      _builder.clearMethodId()
    }

    /**
     * ```
     * 调用该方法所需的 Permission id（如 nervus.permission.manipulator.control）。
     * 空 = 接口级 Resolve 已足够，method 级不再额外要权限（如只读状态查询）。
     *
     * 这是 method 级权限的【真相源】：B1 dispatch 据此在转发前做 method 级裁决。
     * ```
     *
     * `string required_permission = 2 [json_name = "requiredPermission"];`
     */
    public var requiredPermission: kotlin.String
      @JvmName("getRequiredPermission")
      get() = _builder.requiredPermission
      @JvmName("setRequiredPermission")
      set(value) {
        _builder.requiredPermission = value
      }
    /**
     * ```
     * 调用该方法所需的 Permission id（如 nervus.permission.manipulator.control）。
     * 空 = 接口级 Resolve 已足够，method 级不再额外要权限（如只读状态查询）。
     *
     * 这是 method 级权限的【真相源】：B1 dispatch 据此在转发前做 method 级裁决。
     * ```
     *
     * `string required_permission = 2 [json_name = "requiredPermission"];`
     */
    public fun clearRequiredPermission() {
      _builder.clearRequiredPermission()
    }

    /**
     * ```
     * 该方法的风险级，喂给 Agent 工具目录与系统确认框（见 RiskClass 注释）。
     * ```
     *
     * `.nervus.ipc.v1.RiskClass risk_class = 3 [json_name = "riskClass"];`
     */
    public var riskClass: io.github.nervusos.ipc.v1.RiskClass
      @JvmName("getRiskClass")
      get() = _builder.riskClass
      @JvmName("setRiskClass")
      set(value) {
        _builder.riskClass = value
      }
    public var riskClassValue: kotlin.Int
      @JvmName("getRiskClassValue")
      get() = _builder.riskClassValue
      @JvmName("setRiskClassValue")
      set(value) {
        _builder.riskClassValue = value
      }
    /**
     * ```
     * 该方法的风险级，喂给 Agent 工具目录与系统确认框（见 RiskClass 注释）。
     * ```
     *
     * `.nervus.ipc.v1.RiskClass risk_class = 3 [json_name = "riskClass"];`
     */
    public fun clearRiskClass() {
      _builder.clearRiskClass()
    }

    /**
     * ```
     * 是否要求调用方持有该 Resource 的 ControlLease 才能调用。
     * 只读方法为 false；驱动执行器的方法为 true。
     * ```
     *
     * `bool requires_control_lease = 4 [json_name = "requiresControlLease"];`
     */
    public var requiresControlLease: kotlin.Boolean
      @JvmName("getRequiresControlLease")
      get() = _builder.requiresControlLease
      @JvmName("setRequiresControlLease")
      set(value) {
        _builder.requiresControlLease = value
      }
    /**
     * ```
     * 是否要求调用方持有该 Resource 的 ControlLease 才能调用。
     * 只读方法为 false；驱动执行器的方法为 true。
     * ```
     *
     * `bool requires_control_lease = 4 [json_name = "requiresControlLease"];`
     */
    public fun clearRequiresControlLease() {
      _builder.clearRequiresControlLease()
    }

    /**
     * ```
     * true → 该方法是长任务，dispatch 走 operation.Create（B5 Operation Manager），
     * 成功以 STATUS_CODE_ACCEPTED + operation_id 应答，终态另行查询/订阅。
     * false → 普通 request/response，成功以 OK 应答。
     * ```
     *
     * `bool returns_operation = 5 [json_name = "returnsOperation"];`
     */
    public var returnsOperation: kotlin.Boolean
      @JvmName("getReturnsOperation")
      get() = _builder.returnsOperation
      @JvmName("setReturnsOperation")
      set(value) {
        _builder.returnsOperation = value
      }
    /**
     * ```
     * true → 该方法是长任务，dispatch 走 operation.Create（B5 Operation Manager），
     * 成功以 STATUS_CODE_ACCEPTED + operation_id 应答，终态另行查询/订阅。
     * false → 普通 request/response，成功以 OK 应答。
     * ```
     *
     * `bool returns_operation = 5 [json_name = "returnsOperation"];`
     */
    public fun clearReturnsOperation() {
      _builder.clearReturnsOperation()
    }

    /**
     * ```
     * Agent 工具元数据：调用前是否需要【系统持有的】用户确认框。
     *
     * 危险动作（如机械臂移动）应置 true。Agent Service 的 MCP adapter【只投影
     * 不产生】这个标记，Provider 也【不能自报调低】（Agent 文档 §4.3，A4 §3）。
     * ```
     *
     * `bool needs_user_confirmation = 6 [json_name = "needsUserConfirmation"];`
     */
    public var needsUserConfirmation: kotlin.Boolean
      @JvmName("getNeedsUserConfirmation")
      get() = _builder.needsUserConfirmation
      @JvmName("setNeedsUserConfirmation")
      set(value) {
        _builder.needsUserConfirmation = value
      }
    /**
     * ```
     * Agent 工具元数据：调用前是否需要【系统持有的】用户确认框。
     *
     * 危险动作（如机械臂移动）应置 true。Agent Service 的 MCP adapter【只投影
     * 不产生】这个标记，Provider 也【不能自报调低】（Agent 文档 §4.3，A4 §3）。
     * ```
     *
     * `bool needs_user_confirmation = 6 [json_name = "needsUserConfirmation"];`
     */
    public fun clearNeedsUserConfirmation() {
      _builder.clearNeedsUserConfirmation()
    }

    /**
     * ```
     * 请求消息的 Protobuf 全名，如
     * "nervus.interface.manipulator.v1.MoveToPoseRequest"。
     * SDK/Agent 据此定位请求类型；空表示无请求体（极少见）。
     * ```
     *
     * `string request_type = 7 [json_name = "requestType"];`
     */
    public var requestType: kotlin.String
      @JvmName("getRequestType")
      get() = _builder.requestType
      @JvmName("setRequestType")
      set(value) {
        _builder.requestType = value
      }
    /**
     * ```
     * 请求消息的 Protobuf 全名，如
     * "nervus.interface.manipulator.v1.MoveToPoseRequest"。
     * SDK/Agent 据此定位请求类型；空表示无请求体（极少见）。
     * ```
     *
     * `string request_type = 7 [json_name = "requestType"];`
     */
    public fun clearRequestType() {
      _builder.clearRequestType()
    }

    /**
     * ```
     * 响应/终态消息的 Protobuf 全名。
     * returns_operation=false → 普通 Success payload 的类型（可空 = 空 success）
     * returns_operation=true  → operation 成功 result 的类型
     * ```
     *
     * `string response_type = 8 [json_name = "responseType"];`
     */
    public var responseType: kotlin.String
      @JvmName("getResponseType")
      get() = _builder.responseType
      @JvmName("setResponseType")
      set(value) {
        _builder.responseType = value
      }
    /**
     * ```
     * 响应/终态消息的 Protobuf 全名。
     * returns_operation=false → 普通 Success payload 的类型（可空 = 空 success）
     * returns_operation=true  → operation 成功 result 的类型
     * ```
     *
     * `string response_type = 8 [json_name = "responseType"];`
     */
    public fun clearResponseType() {
      _builder.clearResponseType()
    }

    /**
     * ```
     * 是否为只读方法（不改任何状态、无副作用）。Agent 工具目录据此把「安全查询」
     * 与「有副作用的动作」分开呈现给模型；GetXxxState / SubscribeXxx 为 true。
     * ```
     *
     * `bool is_read_only = 9 [json_name = "isReadOnly"];`
     */
    public var isReadOnly: kotlin.Boolean
      @JvmName("getIsReadOnly")
      get() = _builder.isReadOnly
      @JvmName("setIsReadOnly")
      set(value) {
        _builder.isReadOnly = value
      }
    /**
     * ```
     * 是否为只读方法（不改任何状态、无副作用）。Agent 工具目录据此把「安全查询」
     * 与「有副作用的动作」分开呈现给模型；GetXxxState / SubscribeXxx 为 true。
     * ```
     *
     * `bool is_read_only = 9 [json_name = "isReadOnly"];`
     */
    public fun clearIsReadOnly() {
      _builder.clearIsReadOnly()
    }

    /**
     * ```
     * 是否会驱动执行器产生物理运动。供 Agent/确认框/UI 对「会不会动起来」做快速判断。
     * 通常与 risk_class = RISK_CLASS_PHYSICAL_CONTROL 同现，但把它显式化避免消费者
     * 反推风险级。
     * ```
     *
     * `bool is_motion = 10 [json_name = "isMotion"];`
     */
    public var isMotion: kotlin.Boolean
      @JvmName("getIsMotion")
      get() = _builder.isMotion
      @JvmName("setIsMotion")
      set(value) {
        _builder.isMotion = value
      }
    /**
     * ```
     * 是否会驱动执行器产生物理运动。供 Agent/确认框/UI 对「会不会动起来」做快速判断。
     * 通常与 risk_class = RISK_CLASS_PHYSICAL_CONTROL 同现，但把它显式化避免消费者
     * 反推风险级。
     * ```
     *
     * `bool is_motion = 10 [json_name = "isMotion"];`
     */
    public fun clearIsMotion() {
      _builder.clearIsMotion()
    }
  }
}
@kotlin.jvm.JvmSynthetic
public inline fun io.github.nervusos.ipc.v1.MethodMeta.copy(block: `io.github.nervusos.ipc.v1`.MethodMetaKt.Dsl.() -> kotlin.Unit): io.github.nervusos.ipc.v1.MethodMeta =
  `io.github.nervusos.ipc.v1`.MethodMetaKt.Dsl._create(this.toBuilder()).apply { block() }._build()

