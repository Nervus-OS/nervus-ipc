// Generated by the protocol buffer compiler. DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: nervus/ipc/v1/envelope.proto

// Generated files should ignore deprecation warnings
@file:Suppress("DEPRECATION")
package io.github.nervusos.ipc.v1;

@kotlin.jvm.JvmName("-initializerequest")
public inline fun request(block: io.github.nervusos.ipc.v1.RequestKt.Dsl.() -> kotlin.Unit): io.github.nervusos.ipc.v1.Request =
  io.github.nervusos.ipc.v1.RequestKt.Dsl._create(io.github.nervusos.ipc.v1.Request.newBuilder()).apply { block() }._build()
/**
 * ```
 * Request 是调用方发起的一次 RPC。
 *
 * 每个 Request 最终【最多只有一个】终结 Response（§10.7）。这条不变量是
 * route 表、超时、取消与迟到 DispatchResult 丢弃规则共同保证的，不是自然成立的。
 * ```
 *
 * Protobuf type `nervus.ipc.v1.Request`
 */
public object RequestKt {
  @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
  @com.google.protobuf.kotlin.ProtoDslMarker
  public class Dsl private constructor(
    private val _builder: io.github.nervusos.ipc.v1.Request.Builder
  ) {
    public companion object {
      @kotlin.jvm.JvmSynthetic
    @kotlin.PublishedApi
      internal fun _create(builder: io.github.nervusos.ipc.v1.Request.Builder): Dsl = Dsl(builder)
    }

    @kotlin.jvm.JvmSynthetic
  @kotlin.PublishedApi
    internal fun _build(): io.github.nervusos.ipc.v1.Request = _builder.build()

    /**
     * ```
     * 调用者 SDK 在当前连接内生成的原子递增值（§10.6）。
     *
     * 【0 永久保留】，首个请求为 1，绝不回绕：达到上限时关闭并重建连接。
     * 匹配键是 (内部 connection 对象, request_id)，所以不同连接上的相同数字
     * 互不冲突，即使两个进程同属一个 Package、共享 UID。
     *
     * 同一连接内出现重复的、仍在处理中的 request_id 是协议违规。
     * ```
     *
     * `uint64 request_id = 1 [json_name = "requestId"];`
     */
    public var requestId: kotlin.Long
      @JvmName("getRequestId")
      get() = _builder.requestId
      @JvmName("setRequestId")
      set(value) {
        _builder.requestId = value
      }
    /**
     * ```
     * 调用者 SDK 在当前连接内生成的原子递增值（§10.6）。
     *
     * 【0 永久保留】，首个请求为 1，绝不回绕：达到上限时关闭并重建连接。
     * 匹配键是 (内部 connection 对象, request_id)，所以不同连接上的相同数字
     * 互不冲突，即使两个进程同属一个 Package、共享 UID。
     *
     * 同一连接内出现重复的、仍在处理中的 request_id 是协议违规。
     * ```
     *
     * `uint64 request_id = 1 [json_name = "requestId"];`
     */
    public fun clearRequestId() {
      _builder.clearRequestId()
    }

    /**
     * ```
     * 由 ResolveEndpoint 取得，本连接作用域。
     * ```
     *
     * `uint64 endpoint_id = 2 [json_name = "endpointId"];`
     */
    public var endpointId: kotlin.Long
      @JvmName("getEndpointId")
      get() = _builder.endpointId
      @JvmName("setEndpointId")
      set(value) {
        _builder.endpointId = value
      }
    /**
     * ```
     * 由 ResolveEndpoint 取得，本连接作用域。
     * ```
     *
     * `uint64 endpoint_id = 2 [json_name = "endpointId"];`
     */
    public fun clearEndpointId() {
      _builder.clearEndpointId()
    }

    /**
     * ```
     * 接口 schema 中稳定的数字方法 ID。发布后保持稳定，删除后同样保留不用。
     * ```
     *
     * `uint32 method_id = 3 [json_name = "methodId"];`
     */
    public var methodId: kotlin.Int
      @JvmName("getMethodId")
      get() = _builder.methodId
      @JvmName("setMethodId")
      set(value) {
        _builder.methodId = value
      }
    /**
     * ```
     * 接口 schema 中稳定的数字方法 ID。发布后保持稳定，删除后同样保留不用。
     * ```
     *
     * `uint32 method_id = 3 [json_name = "methodId"];`
     */
    public fun clearMethodId() {
      _builder.clearMethodId()
    }

    /**
     * ```
     * 调用者期望的超时（毫秒）。
     *
     * 【0 表示使用方法默认值，不表示无限】。nervud 按方法最大值收紧，并在接收
     * 时立刻转换成本机【单调时钟】deadline——用墙钟会让一次 NTP 校时把全部
     * 在途调用推向立即超时或永不超时。
     * ```
     *
     * `uint32 timeout_ms = 4 [json_name = "timeoutMs"];`
     */
    public var timeoutMs: kotlin.Int
      @JvmName("getTimeoutMs")
      get() = _builder.timeoutMs
      @JvmName("setTimeoutMs")
      set(value) {
        _builder.timeoutMs = value
      }
    /**
     * ```
     * 调用者期望的超时（毫秒）。
     *
     * 【0 表示使用方法默认值，不表示无限】。nervud 按方法最大值收紧，并在接收
     * 时立刻转换成本机【单调时钟】deadline——用墙钟会让一次 NTP 校时把全部
     * 在途调用推向立即超时或永不超时。
     * ```
     *
     * `uint32 timeout_ms = 4 [json_name = "timeoutMs"];`
     */
    public fun clearTimeoutMs() {
      _builder.clearTimeoutMs()
    }

    /**
     * ```
     * 方法专属 Protobuf 请求的序列化 bytes。类型由
     * endpoint_id + 选中 interface_version + method_id 唯一决定。
     * ```
     *
     * `bytes payload = 5 [json_name = "payload"];`
     */
    public var payload: com.google.protobuf.ByteString
      @JvmName("getPayload")
      get() = _builder.payload
      @JvmName("setPayload")
      set(value) {
        _builder.payload = value
      }
    /**
     * ```
     * 方法专属 Protobuf 请求的序列化 bytes。类型由
     * endpoint_id + 选中 interface_version + method_id 唯一决定。
     * ```
     *
     * `bytes payload = 5 [json_name = "payload"];`
     */
    public fun clearPayload() {
      _builder.clearPayload()
    }
  }
}
@kotlin.jvm.JvmSynthetic
public inline fun io.github.nervusos.ipc.v1.Request.copy(block: `io.github.nervusos.ipc.v1`.RequestKt.Dsl.() -> kotlin.Unit): io.github.nervusos.ipc.v1.Request =
  `io.github.nervusos.ipc.v1`.RequestKt.Dsl._create(this.toBuilder()).apply { block() }._build()

