// Generated by the protocol buffer compiler. DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: nervus/ipc/v1/status.proto

// Generated files should ignore deprecation warnings
@file:Suppress("DEPRECATION")
package io.github.nervusos.ipc.v1;

@kotlin.jvm.JvmName("-initializefailure")
public inline fun failure(block: io.github.nervusos.ipc.v1.FailureKt.Dsl.() -> kotlin.Unit): io.github.nervusos.ipc.v1.Failure =
  io.github.nervusos.ipc.v1.FailureKt.Dsl._create(io.github.nervusos.ipc.v1.Failure.newBuilder()).apply { block() }._build()
/**
 * ```
 * Failure 是唯一的失败分支。
 *
 * 不变量（接收方必须校验）：
 *
 * code ∉ {UNSPECIFIED, OK, ACCEPTED}
 *
 * 任何非法组合一律 fail closed 为协议违规 / INTERNAL。
 * ```
 *
 * Protobuf type `nervus.ipc.v1.Failure`
 */
public object FailureKt {
  @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
  @com.google.protobuf.kotlin.ProtoDslMarker
  public class Dsl private constructor(
    private val _builder: io.github.nervusos.ipc.v1.Failure.Builder
  ) {
    public companion object {
      @kotlin.jvm.JvmSynthetic
    @kotlin.PublishedApi
      internal fun _create(builder: io.github.nervusos.ipc.v1.Failure.Builder): Dsl = Dsl(builder)
    }

    @kotlin.jvm.JvmSynthetic
  @kotlin.PublishedApi
    internal fun _build(): io.github.nervusos.ipc.v1.Failure = _builder.build()

    /**
     * `.nervus.ipc.v1.StatusCode code = 1 [json_name = "code"];`
     */
    public var code: io.github.nervusos.ipc.v1.StatusCode
      @JvmName("getCode")
      get() = _builder.code
      @JvmName("setCode")
      set(value) {
        _builder.code = value
      }
    public var codeValue: kotlin.Int
      @JvmName("getCodeValue")
      get() = _builder.codeValue
      @JvmName("setCodeValue")
      set(value) {
        _builder.codeValue = value
      }
    /**
     * `.nervus.ipc.v1.StatusCode code = 1 [json_name = "code"];`
     */
    public fun clearCode() {
      _builder.clearCode()
    }

    /**
     * ```
     * 仅供人阅读，【不参与程序判断】。
     *
     * 禁止原样透传 Service/Provider 的自由文本：nervud 只能从受审计模板和
     * 已脱敏的结构化字段生成，并施加长度上限（由 nervud 实现强制，schema
     * 无法表达）。程序不得对本字段做 switch，也不得据它本地化分支。
     * ```
     *
     * `string public_message = 2 [json_name = "publicMessage"];`
     */
    public var publicMessage: kotlin.String
      @JvmName("getPublicMessage")
      get() = _builder.publicMessage
      @JvmName("setPublicMessage")
      set(value) {
        _builder.publicMessage = value
      }
    /**
     * ```
     * 仅供人阅读，【不参与程序判断】。
     *
     * 禁止原样透传 Service/Provider 的自由文本：nervud 只能从受审计模板和
     * 已脱敏的结构化字段生成，并施加长度上限（由 nervud 实现强制，schema
     * 无法表达）。程序不得对本字段做 switch，也不得据它本地化分支。
     * ```
     *
     * `string public_message = 2 [json_name = "publicMessage"];`
     */
    public fun clearPublicMessage() {
      _builder.clearPublicMessage()
    }

    /**
     * ```
     * 可选的类型化细因，序列化后的 Protobuf bytes。
     *
     * 类型由【外层消息类型】决定，不由本字段自述：
     * 普通 Response/DispatchResult → interface_version + method_id 决定
     * ResolveEndpointResult        → ResolveEndpointErrorDetail
     * 其余 *Result                 → 各自消息类型固定的 detail 类型
     *
     * nervud 必须按权威 method schema 解码 Service 提交的 detail，校验
     * (code, reason) 允许映射、字段长度、枚举值域，丢弃未知字段后【重新编码】
     * 再转发——不得把 Service 的原始 bytes 直接透传给调用者（§10.7）。
     * ```
     *
     * `bytes error_detail = 3 [json_name = "errorDetail"];`
     */
    public var errorDetail: com.google.protobuf.ByteString
      @JvmName("getErrorDetail")
      get() = _builder.errorDetail
      @JvmName("setErrorDetail")
      set(value) {
        _builder.errorDetail = value
      }
    /**
     * ```
     * 可选的类型化细因，序列化后的 Protobuf bytes。
     *
     * 类型由【外层消息类型】决定，不由本字段自述：
     * 普通 Response/DispatchResult → interface_version + method_id 决定
     * ResolveEndpointResult        → ResolveEndpointErrorDetail
     * 其余 *Result                 → 各自消息类型固定的 detail 类型
     *
     * nervud 必须按权威 method schema 解码 Service 提交的 detail，校验
     * (code, reason) 允许映射、字段长度、枚举值域，丢弃未知字段后【重新编码】
     * 再转发——不得把 Service 的原始 bytes 直接透传给调用者（§10.7）。
     * ```
     *
     * `bytes error_detail = 3 [json_name = "errorDetail"];`
     */
    public fun clearErrorDetail() {
      _builder.clearErrorDetail()
    }
  }
}
@kotlin.jvm.JvmSynthetic
public inline fun io.github.nervusos.ipc.v1.Failure.copy(block: `io.github.nervusos.ipc.v1`.FailureKt.Dsl.() -> kotlin.Unit): io.github.nervusos.ipc.v1.Failure =
  `io.github.nervusos.ipc.v1`.FailureKt.Dsl._create(this.toBuilder()).apply { block() }._build()

