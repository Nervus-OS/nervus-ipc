// Generated by the protocol buffer compiler. DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: nervus/ipc/v1/envelope.proto

// Generated files should ignore deprecation warnings
@file:Suppress("DEPRECATION")
package io.github.nervusos.ipc.v1;

@kotlin.jvm.JvmName("-initializeconnectionLimits")
public inline fun connectionLimits(block: io.github.nervusos.ipc.v1.ConnectionLimitsKt.Dsl.() -> kotlin.Unit): io.github.nervusos.ipc.v1.ConnectionLimits =
  io.github.nervusos.ipc.v1.ConnectionLimitsKt.Dsl._create(io.github.nervusos.ipc.v1.ConnectionLimits.newBuilder()).apply { block() }._build()
/**
 * ```
 * ConnectionLimits 是 §10.11 准入限制在 wire 上的投影。
 *
 * 服务端【不】因为告知过就放松执法：这些值是 SDK 的自律依据，不是承诺。
 * 实际取值可能因调用者 profile 而不同，但都不超过 128 KiB Frame 硬上限与
 * 进程级全局预算，不存在「无限」档位。
 * ```
 *
 * Protobuf type `nervus.ipc.v1.ConnectionLimits`
 */
public object ConnectionLimitsKt {
  @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
  @com.google.protobuf.kotlin.ProtoDslMarker
  public class Dsl private constructor(
    private val _builder: io.github.nervusos.ipc.v1.ConnectionLimits.Builder
  ) {
    public companion object {
      @kotlin.jvm.JvmSynthetic
    @kotlin.PublishedApi
      internal fun _create(builder: io.github.nervusos.ipc.v1.ConnectionLimits.Builder): Dsl = Dsl(builder)
    }

    @kotlin.jvm.JvmSynthetic
  @kotlin.PublishedApi
    internal fun _build(): io.github.nervusos.ipc.v1.ConnectionLimits = _builder.build()

    /**
     * ```
     * 绝对 Frame 上限，恒为 131072（128 KiB）。作为字段下发是为了让 SDK
     * 有单一真相源，而不是把 128 KiB 硬编码在两种语言的三个地方。
     * ```
     *
     * `uint32 max_frame_bytes = 1 [json_name = "maxFrameBytes"];`
     */
    public var maxFrameBytes: kotlin.Int
      @JvmName("getMaxFrameBytes")
      get() = _builder.maxFrameBytes
      @JvmName("setMaxFrameBytes")
      set(value) {
        _builder.maxFrameBytes = value
      }
    /**
     * ```
     * 绝对 Frame 上限，恒为 131072（128 KiB）。作为字段下发是为了让 SDK
     * 有单一真相源，而不是把 128 KiB 硬编码在两种语言的三个地方。
     * ```
     *
     * `uint32 max_frame_bytes = 1 [json_name = "maxFrameBytes"];`
     */
    public fun clearMaxFrameBytes() {
      _builder.clearMaxFrameBytes()
    }

    /**
     * ```
     * 普通方法默认 request/response 额度（§10.3：16 KiB）。
     * 逐方法额度在 Registry 中声明，最终取「方法额度、profile 额度、硬上限」最小值。
     * ```
     *
     * `uint32 default_method_payload_bytes = 2 [json_name = "defaultMethodPayloadBytes"];`
     */
    public var defaultMethodPayloadBytes: kotlin.Int
      @JvmName("getDefaultMethodPayloadBytes")
      get() = _builder.defaultMethodPayloadBytes
      @JvmName("setDefaultMethodPayloadBytes")
      set(value) {
        _builder.defaultMethodPayloadBytes = value
      }
    /**
     * ```
     * 普通方法默认 request/response 额度（§10.3：16 KiB）。
     * 逐方法额度在 Registry 中声明，最终取「方法额度、profile 额度、硬上限」最小值。
     * ```
     *
     * `uint32 default_method_payload_bytes = 2 [json_name = "defaultMethodPayloadBytes"];`
     */
    public fun clearDefaultMethodPayloadBytes() {
      _builder.clearDefaultMethodPayloadBytes()
    }

    /**
     * ```
     * 每连接最大 in-flight Request 数（默认 64）。
     * ```
     *
     * `uint32 max_inflight_requests = 3 [json_name = "maxInflightRequests"];`
     */
    public var maxInflightRequests: kotlin.Int
      @JvmName("getMaxInflightRequests")
      get() = _builder.maxInflightRequests
      @JvmName("setMaxInflightRequests")
      set(value) {
        _builder.maxInflightRequests = value
      }
    /**
     * ```
     * 每连接最大 in-flight Request 数（默认 64）。
     * ```
     *
     * `uint32 max_inflight_requests = 3 [json_name = "maxInflightRequests"];`
     */
    public fun clearMaxInflightRequests() {
      _builder.clearMaxInflightRequests()
    }

    /**
     * ```
     * 每连接 in-flight payload 总字节（默认 1 MiB）。
     * 【按字节计量而不是条数】：只数条数的队列会被 64 个 128 KiB 请求撑爆。
     * ```
     *
     * `uint64 max_inflight_payload_bytes = 4 [json_name = "maxInflightPayloadBytes"];`
     */
    public var maxInflightPayloadBytes: kotlin.Long
      @JvmName("getMaxInflightPayloadBytes")
      get() = _builder.maxInflightPayloadBytes
      @JvmName("setMaxInflightPayloadBytes")
      set(value) {
        _builder.maxInflightPayloadBytes = value
      }
    /**
     * ```
     * 每连接 in-flight payload 总字节（默认 1 MiB）。
     * 【按字节计量而不是条数】：只数条数的队列会被 64 个 128 KiB 请求撑爆。
     * ```
     *
     * `uint64 max_inflight_payload_bytes = 4 [json_name = "maxInflightPayloadBytes"];`
     */
    public fun clearMaxInflightPayloadBytes() {
      _builder.clearMaxInflightPayloadBytes()
    }

    /**
     * ```
     * 每连接 outbound queue 总字节（默认 512 KiB）。超出且无法安全排入响应时
     * 关闭慢消费者。
     * ```
     *
     * `uint64 max_outbound_queue_bytes = 5 [json_name = "maxOutboundQueueBytes"];`
     */
    public var maxOutboundQueueBytes: kotlin.Long
      @JvmName("getMaxOutboundQueueBytes")
      get() = _builder.maxOutboundQueueBytes
      @JvmName("setMaxOutboundQueueBytes")
      set(value) {
        _builder.maxOutboundQueueBytes = value
      }
    /**
     * ```
     * 每连接 outbound queue 总字节（默认 512 KiB）。超出且无法安全排入响应时
     * 关闭慢消费者。
     * ```
     *
     * `uint64 max_outbound_queue_bytes = 5 [json_name = "maxOutboundQueueBytes"];`
     */
    public fun clearMaxOutboundQueueBytes() {
      _builder.clearMaxOutboundQueueBytes()
    }

    /**
     * ```
     * 每连接最大并发订阅数。
     * ```
     *
     * `uint32 max_subscriptions = 6 [json_name = "maxSubscriptions"];`
     */
    public var maxSubscriptions: kotlin.Int
      @JvmName("getMaxSubscriptions")
      get() = _builder.maxSubscriptions
      @JvmName("setMaxSubscriptions")
      set(value) {
        _builder.maxSubscriptions = value
      }
    /**
     * ```
     * 每连接最大并发订阅数。
     * ```
     *
     * `uint32 max_subscriptions = 6 [json_name = "maxSubscriptions"];`
     */
    public fun clearMaxSubscriptions() {
      _builder.clearMaxSubscriptions()
    }

    /**
     * ```
     * Request.timeout_ms 为 0 时采用的默认值，以及服务端允许的上限。
     * nervud 按方法最大值收紧调用者请求的 timeout，两者取小。
     * ```
     *
     * `uint32 default_timeout_ms = 7 [json_name = "defaultTimeoutMs"];`
     */
    public var defaultTimeoutMs: kotlin.Int
      @JvmName("getDefaultTimeoutMs")
      get() = _builder.defaultTimeoutMs
      @JvmName("setDefaultTimeoutMs")
      set(value) {
        _builder.defaultTimeoutMs = value
      }
    /**
     * ```
     * Request.timeout_ms 为 0 时采用的默认值，以及服务端允许的上限。
     * nervud 按方法最大值收紧调用者请求的 timeout，两者取小。
     * ```
     *
     * `uint32 default_timeout_ms = 7 [json_name = "defaultTimeoutMs"];`
     */
    public fun clearDefaultTimeoutMs() {
      _builder.clearDefaultTimeoutMs()
    }

    /**
     * `uint32 max_timeout_ms = 8 [json_name = "maxTimeoutMs"];`
     */
    public var maxTimeoutMs: kotlin.Int
      @JvmName("getMaxTimeoutMs")
      get() = _builder.maxTimeoutMs
      @JvmName("setMaxTimeoutMs")
      set(value) {
        _builder.maxTimeoutMs = value
      }
    /**
     * `uint32 max_timeout_ms = 8 [json_name = "maxTimeoutMs"];`
     */
    public fun clearMaxTimeoutMs() {
      _builder.clearMaxTimeoutMs()
    }

    /**
     * ```
     * 服务端在多久没有任何 Frame 后判定连接空闲。SDK 据此决定 Ping 周期，
     * 不必猜测。
     * ```
     *
     * `uint32 idle_timeout_ms = 9 [json_name = "idleTimeoutMs"];`
     */
    public var idleTimeoutMs: kotlin.Int
      @JvmName("getIdleTimeoutMs")
      get() = _builder.idleTimeoutMs
      @JvmName("setIdleTimeoutMs")
      set(value) {
        _builder.idleTimeoutMs = value
      }
    /**
     * ```
     * 服务端在多久没有任何 Frame 后判定连接空闲。SDK 据此决定 Ping 周期，
     * 不必猜测。
     * ```
     *
     * `uint32 idle_timeout_ms = 9 [json_name = "idleTimeoutMs"];`
     */
    public fun clearIdleTimeoutMs() {
      _builder.clearIdleTimeoutMs()
    }
  }
}
@kotlin.jvm.JvmSynthetic
public inline fun io.github.nervusos.ipc.v1.ConnectionLimits.copy(block: `io.github.nervusos.ipc.v1`.ConnectionLimitsKt.Dsl.() -> kotlin.Unit): io.github.nervusos.ipc.v1.ConnectionLimits =
  `io.github.nervusos.ipc.v1`.ConnectionLimitsKt.Dsl._create(this.toBuilder()).apply { block() }._build()

